# OFFENE-REVIEW-PUNKTE — verworfene False-Positives (Reviewer-Gedächtnis)

> Zweck (Helfer V4.1): Findings, die nach Stufe-3-Verifikation bewusst NICHT gefixt wurden.
> Pflicht-Anhang für jeden Review-Prompt, damit dieselben Punkte nicht in jeder Welle neu aufschlagen.

| # | Datum | Artefakt | Finding (Reviewer) | Warum verworfen |
|---|-------|----------|--------------------|-----------------|
| 1 | 17.07.2026 | party-worker.js (Welle 8, Nr. 10) | og:site_name „mach'sleicht" → „mach's leicht" | „mach'sleicht" ist die site-weite og:site_name-Konvention (index.html Z. 19, einladung/index.html u. a.). Nur den Worker zu ändern würde Inkonsistenz ERZEUGEN. Falls Bolle die Leerzeichen-Schreibweise will: site-weiter Sweep als eigenes PBI. |
| 2 | 17.07.2026 | Studio-Autopilot (Welle A1, Nr. 11) | autopilot_open-Undercount bei Same-Tab-Navigation (fetch vom Unload gekillt) | Bekannte Grenze aller onclick-Tracker. Primärmetrik ist studio_open{autopilot:true} + autopilot_shown (verlustfrei im Studio-Boot). autopilot_open bleibt als Intent-Zusatz. |
| 3 | 17.07.2026 | party-worker.js (Welle 10, Nr. 8) | 409-Existenz-Orakel: unbestätigter rsvp-POST verrät pro Name, ob ein Walk-in geantwortet hat | Designbedingt am Kollisionsschutz (Namens-Vertrauensmodell wie RSVP selbst, 90/h gedrosselt). Als akzeptiert im Code kommentiert (W10-8); Alternative würde den Warn-Dialog kosten. |
| 4 | 17.07.2026 | Studio-Autopilot (Welle A4, Nr. 1, Foto-Teil) | „Foto-Fokus deckelt auf 38/12/27" | Am Code widerlegt: die applyLayout-Fits kennen nur die Weiche `name==="clean"?x:y` — photo nutzt exakt die classic-Maxima (42/14/30). Nur der Clean-Teil des Findings war echt (gefixt in A4-1). |
| 5 | 17.07.2026 | party-worker.js (Welle 11, Nr. 2) | Invite-Gast kann Allergie/Abholung vom Zweitgerät nicht LEEREN (Erben greift) | Bewusster Trade-off statt Reviewer-Fix: dessen 409+leer→null-Weg würde den häufigen Fall (Statuswechsel vom Zweitgerät, Felder dort systembedingt leer) nach einem harmlos klingenden Confirm in VERSEHENTLICHES Löschen verwandeln — Erben ist die sicherere Vorgabe. Löschen geht übers Erstgerät oder den Host-Editor. Sauberer Fix (Server-Prefill der eigenen Werte für Token-Gäste, Identität ist tokensicher) → Backlog. **UPDATE 17.07.: gelöst als W14 (Server-Prefill + defaultValue-Löschsignal), s. #6.** |
| 7 | 18.07.2026 | einladung/studio/index.html (W15-Review, Opus, MAJOR-C/F4) | `fitDownToWidth` (Export) setzt nowrap + schrumpft dauerhaft → angeblich „mehrzeiliger mission-Text wird im PNG zur geschrumpften Einzeile" (WYSIWYG-Bruch) | Am Code UND per Live-Playtest widerlegt: `fitDownToWidth` läuft im Export nur auf `headline`/`mission`/`invite` — und exakt diese drei werden im Normalpfad mit `fitSingleLine` gerendert (applyData Z.967-973, applyLayout Z.1116-1118), das selbst `whiteSpace="nowrap"` setzt (Z.1292). Die Live-Karte ist also ohnehin nowrap-einzeilig; der Export bestätigt nur denselben Fit. Nur `message` ist mehrzeilig (`fitWrappedText`), und darauf ruft der Export `fitDownToWidth` NICHT auf. **Playtest (served, 18.07.):** computed `white-space` = nowrap für headline/mission/invite, pre-wrap nur für message; Export-Klick → Toast „PNG gespeichert ✓", 0 Konsolen-Fehler, mission bleibt nowrap+passt. Kein WYSIWYG-Bruch, kein Fix nötig. Rest des Follow-up-Tickets (Share-Hang-Guard) = minor/pre-existing/nicht dringend. |
| 6 | 17.07.2026 | party-worker.js (W14-Review, Opus) | Cross-Gast-Read: `curl -d '{"g":"T2","status":"ja"}'` mit WEGGELASSENEM Allergiefeld → Server adoptiert gleichnamige Waise (Z.578) + erbt deren Allergie (Z.582) → beim Reload prefillt Z.1967 die fremde Allergie. Bei zwei VERSCHIEDENEN Kindern namens „Max" liest T2 fremde Gesundheitsdaten. | Bewusst NICHT gefixt (akzeptierte schmale Schuld): (a) NUR per handgebautem Request erreichbar — der reale Client (2109-2111) sendet immer al.value oder null, nie `undefined`; (b) verlangt zwei gleichnamige VERSCHIEDENE Kinder + Invite-Löschen+Neuanlegen; (c) gehört zum bereits akzeptierten **Namens-Vertrauensmodell** (Z.578-Adoption, wie #3). Der naheliegende Fix (Erben nur bei Exact-Token, nicht bei Namens-Adoption) würde die legitime Same-Person-Adoption brechen (gleicher Mensch, neu-verlinkter Invite → Allergie neu eingeben) — schlechterer Trade als die schmale Rest-Schuld. W14-Fix-1 (defaultValue statt INVITE_TOKEN) schließt den client-erreichbaren Allergie-VERLUST, ohne die Leak-Fläche zu vergrößern (Prefill bleibt exact-token-only). |

| 8 | 19.07.2026 | einladung/studio/index.html (10-Lens-UX-Audit, K4) | „Mobile-Undo unerreichbar — Undo-Button in auf Mobil ausgeblendeten Panels" | Am Code widerlegt: `#undoBtn` sitzt in der `.stage-toolbar`, die auf ≤760px `position:sticky;top:58px` **sichtbar** ist (Z.210/382). `display:none` nur unter `body.autopilot` (Z.154), nicht im normalen Mobil-Editor. Undo ist am Handy nach Fehl-Drag/Löschen erreichbar. Realer K4-Teil (Löschen ohne Toast) gefixt. |
| 9 | 19.07.2026 | einladung/studio/index.html (10-Lens-UX-Audit, K2) | „Autopilot zeigt still Fake-‚fertig'-Demo (Matti) + tote Buttons auf Fremdgerät/Inkognito/ITP-Purge" | Überzeichnet: bei fehlendem localStorage ist `&&localStorage.getItem(PLANNER_KEY)` falsy → Autopilot wird NICHT gesetzt; Z.2147 entfernt die Klasse + Toast „Keine passenden Party-Daten — füll kurz den Planer aus". Die „fertig+tote-Buttons"-Kombi ist nur ein transienter Flash bei UNVOLLSTÄNDIGEM PLANNER_KEY. Realer K2-Kern (Kalt-Start-Demo teilbar via ungeguardetem shareBtn) gefixt via `guardDemoShare`. |
| 10 | 19.07.2026 | einladung/erstellen/index.html (10-Lens-UX-Audit, H5) | „Wizard Schritt 1: `height:90vh`-Drawer, Tastatur schiebt ‚Weiter →' aus dem Bild" | NOT_REPRODUCED in dieser Datei: kein `90vh`, kein „Weiter", kein Drawer/Wizard — der Funnel ist eine einzelne scrollende Card (`<button type=submit>🎉 Einladung erstellen</button>`). Das beschriebene Artefakt gehört zum Planer-Wizard `kindergeburtstag.html` → dort separat prüfen (Welle 3). |
| 11 | 19.07.2026 | einladung/erstellen/index.html (10-Lens-UX-Audit, H1) | „Bild-Einladung hinter E-Mail-Gate; brauner `#D4812A`-Button; Verlustangst-Zeile; Verb ‚Einladung ansehen'" | Teils widerlegt: kein `#D4812A` (grep 0), KEIN E-Mail-Gate in dieser Datei (Partyseite-CTA ist ein `<a>` zu party.machsleicht.de), Verb ist „Vorschau ansehen", keine Verlustangst-Zeile (Copy ist benefit-framed). REALER Rest: `.btn-preview` (15%-Gold, 13px) ist visuell schwächer als die Lead-Gen-Partyseite-CTA (Voll-Gold, 800) → Hierarchie-Fix in Welle 3. |
| 12 | 19.07.2026 | einladung/studio/index.html (10-Lens-UX-Audit, div. Detailclaims) | M2 „Gäste-Zähler <44px" · M4 „speichert `photo:null`, Autopilot baut fotolos ohne Hinweis" · H10 „`role=dialog` ohne Fokusfalle" · N3 „`*{outline:none}` global" | Alle vier am Code widerlegt/überzeichnet: (M2) kein Gäste-Zähler existiert im Studio — reale Sub-44px sind close/delete 34px (gefixt) + resize-handle 22px + native age-Spinner. (M4) atomarer Ein-Key-Save wirft bei Quota und speichert NICHTS (kein partielles `photo:null`); Quota-Fail wird bei `showToast=true` gemeldet. (H10) es gibt gar kein `role=dialog` im File. (N3) `outline:none` ist auf `input,select,textarea` beschränkt, kein globales `*`. Reale Kerne (aria-live, reduced-motion, h1, focus-visible, Layer-Namen) gefixt. |

Ältere False-Positive-Entscheidungen (vor Anlage dieser Datei) leben in SESSION-NOTES-Einträgen, u. a.: shareGuest-\n-Beifang (Design-Welle 6), poss()-Genitiv-Apostroph (Bolle-Stilentscheid, Memory), Umami auf ?g=-Gastseiten (bewusste Funnel-Entscheidung), /api/invphoto-403-Auth (anderes Produkt), KV-Read-modify-write-Race (dokumentierte Schuld, W10).
