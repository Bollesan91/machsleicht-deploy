# Paket-Datenfluss — Herkunfts-Matrix & Hardcode-Register (Stand 11.08.2026)

> Doktrin (Bolle): Jedes gedruckte Ding erklaert sich — Datenquelle oder erklaerte Konstante. Unerklaerte Hardcodes sind schuldig.

> **Status-Nachtrag (Session 11.08., nach Redaktionsschluss der Agenten):**
> 1. Der P0 aus Anhang B (rsvpPhone-Fremdnummer 491701234567 + Phantomort in allen 15 Gast-Apps) ist GEFIXT: Real-Modus-Gate + Render-Guards, Commit 45338572 auf draft, unabhaengiger Review laeuft.
> 2. Die Planerseiten-Blindstelle (Generator liest _src/elite-motto-data, nicht data/motto) wurde vom Baustelle-recheck4 unabhaengig bestaetigt und in Welle 5 (5e95aa79) fuer baustelle geschlossen (Feld-Sync + Regeneration). Die Katalog-Vereinigung bleibt offen (K6).

**Lesehilfe zum mechanischen Vorab-Report** (`_dev/review/2026-08-11-datenfluss-mechanisch.txt`): „NIE GELESEN" dort ist ein **Kandidat**, kein Urteil. Der Report grept Feld-*Literale* im Paket-Code. Drei Konsummuster entziehen sich ihm systematisch — (a) `Object.keys`-Loops (`sosScenarios`), (b) konstruierte Namen (`ageAdjust`+N per Regex), (c) String-Literal-Whitelists (`preparationWeeks`) — und ein vierter Blindspot ist der **Ort**: der Report scannt nur `paket/`, waehrend die Planerseiten (`_src/generate-age-pages.py`) und die Wizard-Runtime (`kindergeburtstag.html`) eigene Renderer sind. Von 62 Kandidaten sind nach Pruefung **11 wirklich tot**.

Alle Zeilennummern beziehen sich auf `paket/baustelle/index.html` (GEBAUTER Index = Konsum-Wahrheit), sofern nicht anders genannt.

---

## 1. Herkunfts-Matrix je Dossier-Blatt

Physische Reihenfolge aus `render()` (Z.977-995), selbst gelesen:
`shCover → shTimeline → shCountdown → shPoster → shShopping → shDecoCake → shGameCards → shStations → shSOS → shCertificates → shTableCards → shGuestsWishes → shInvitations → shHandzettel → shDanke → shLogbuch`

| # | Blatt (sh*-Funktion) | PARTY-Felder | DATA-Felder (data/motto) | Manifest-Woerter | Erklaerte Konstanten |
|---|---|---|---|---|---|
| 1 | **shCover** (Z.429) | `childName`, `guests[].status`, `guestCount`, `age`, `date`, `time` | — | **keine** (`WORTE={}`) | `'Unser Baustellen-Kind'` (Fallback-Name), `'Bautrupp rückt noch an'` (Leerzustand), `coverSvg()`, `'Seite 1'` |
| 2 | **shTimeline** (Z.448) | `date`, `time`, `endTime`, `guests` (via `abholNote()`) | `variants[].timeWindow`, `variants[].games[]` (via `buildTimeline`), `sosScenarios.regen.{label,headline}` | keine | `'Startzeit 14:00 ist angenommen'` (Ehrlichkeits-Ansage bei fehlender Zeit), `'Ende auf 3 Stunden nach Start'`, `'Seite 2 · Ablaufplan'` |
| 3 | **shCountdown** (Z.473) | `date` | `preparationWeeks.{minus6Weeks,minus4Weeks,minus2Weeks,minus1Week,minus2Days,minus1Day,dayOf}.items[].{icon,title,detail}` | keine | **7 order-Labels** (Z.476) + **offs-Tabelle** `-42/-28/-14/-7/-2/-1/0` (Z.481) → **beide ANGEKLAGT, s. §4**; `'Jetzt sofort'`-Kollaps (Kurzentschlossene sehen keine toten Fristen); `'Seite 3 · Countdown'` |
| 4 | **shPoster** (Z.905) | `childName`, `date` | — | keine | **14 Nächte** (Z.909 `for(var i=14;i>=1;i--)`) — Poster-Format, unabhaengig vom echten Datum; `'Noch ⬜ Nächte bis zum Baustart!'`; Footer **ohne** Seitenzahl |
| 5 | **shShopping** (Z.507) | `guests[].status` | `variants[].shoppingList[].{emoji,label,priceEur,url,hasAffiliate}`, `variants[].food`, `variants[].giveaways`, `variants[].costContext` | keine | `Σ`-Summe wird **gerechnet, nie gepflegt** (Z.515-531, begruendet: 47 handgepflegte Summen waren abgedriftet); `/^Optional:/`-Regex als Pflicht/Extra-Trennung; `'Seite 4 · Einkauf & Menü'` → **Nummer ANGEKLAGT** |
| 6 | **shDecoCake** (Z.557) | `guests` (via `allergieZeile()`/`allergieTausch()`) | `variants[].decoration`, `cakeRecipe.{intro,steps[].content,meta.{aufwand,kosten,allergiker,safety}}` | keine | `'🚧 Deko'`, `'🎂 Baustellen-Kuchen'`; `'Seite 5 · Deko & Kuchen'` → **Nummer ANGEKLAGT**. `cakeRecipe.tips[]` fehlt (§2) |
| 7 | **shGameCards** (Z.570) | `age`, `ageGroup`, `guests`, `gameId` | `variants[].games[].{name,material,prepText,ageAdjustN,safetyRule,indoorTip,outdoorTip,kern,quizCards[],alibiTabelle}`, `signatureRitual.{name,introText,setupSteps,rolesList[].{emoji,name,function,abVariante},materialNote,optOutNote,stempelHeft}` | keine | Alters-Baender `_sp` = `[3,5]/[6,8]/[9,12]` (Z.617) mit Begruendung „Fehlender Rat ist besser als falscher" (Z.608-615); `_vr={minimal:0,standard:1,wow:2}` (Z.659); `'Anfang & Ende'`; `'Teil III · Bauplan-Karten'` |
| 8 | **shStations** (Z.835) | `childName`, `ageGroup` | **SCHATZ** (eigene Quelle, nicht data/motto): `stations[grp]`, `intro[grp]` | keine | **Zwei Early-Returns** (Z.836 `if(!SCHATZ)`, Z.839 `if(!sts.length)`) → Blatt kann entfallen; `'Baustellen-Mission-Stationen'` |
| 9 | **shSOS** (Z.685) | — | `sosScenarios.<jeder Key>.{icon,label,headline,steps,fallback}` via `Object.keys` | keine | `'Seite 9 · SOS'` → **ANGEKLAGT** (§4); Fallback `s.label||k` → **LUECKE** (§4) |
| 10 | **shCertificates** (Z.701) | `childName`, `guests[].name` | — | keine | `'Offizielle Richtfest-Urkunde'`, `'Junior-Baumeister'`, kompletter Urkundentext, `siegelSvg()`; **1 Blanko-Reserve** immer (Z.721), **2** im Leerzustand (Z.722, deckt das gedruckte Versprechen aus Z.718) |
| 11 | **shTableCards** (Z.725) | `guests[].name`, `invites[].{n,role}` | — | keine | **6 Zellen Minimum** (Z.727 `while(names.length<6)`), `'Trupp-Mitglied'`, `'Werkzeug für'`, `wachName()` (deterministischer Beiname), `tent`-Falzmarke (begruendet Z.740-745: Knick lief vorher durch den Kindernamen) |
| 12 | **shGuestsWishes** (Z.750) | `guests[].{name,status,allergies,pickupPerson,pickupTime}`, `guestCount`, `wishes[].{title,price,sharedGift}`, `childName`, `editToken` (→`HASTOKEN`) | — | keine | Spaltenkoepfe `Kind / Allergien-Essen / Abholung` (Z.767) → **an `askAllergies`/`askPickup` ungebunden, LUECKE §3**; Public-Mode-Ehrlichkeitstext; `'Küchen-Zettel'` |
| 13 | **shInvitations** (Z.773) | `invites[].{n,role,t}`, `childName`, `date`, `time`, `address`, `PARTYURL` | — | keine | `'dein Party-Pass wartet'` (generisch statt `PASS_TITLES`, §3), `reprint`-Satz (Z.787), `'Einladungen'` |
| 14 | **shHandzettel** (Z.920) | `childName`, `date`, `time`, `endTime`, `address`, `PARTYURL` | — | keine | **4 identische Karten** (bewusst: gehen an beliebige Eltern, Z.938-941); `'✅ Zu-/Absage & Allergien'`; `qrBlock()` statt Roh-URL (Ring 4, 11.08.); `'Eltern-Handzettel'` |
| 15 | **shDanke** (Z.866) | `childName`, `guests[].name` | — | **bewusst keine** — Kommentar Z.877-879: „`G()` lieferte ausserhalb von ritter 'Gruppe' … der WhatsApp-Text ist deshalb bewusst motto-NEUTRAL" | Kompletter `waDank`-WhatsApp-Text; Ausschneide-Dankkarten am 11.08. entfernt (Ring 4, begruendet Z.869-874); `'Danke sagen'` |
| 16 | **shLogbuch** (Z.894) | `childName` | — | keine | **5 feste Fragen** (Z.896) — Keepsake-Formular; `'Bautagebuch'` |

### Befund zur Spalte „Manifest-Woerter": die Vokabel-Schicht ist in 6 von 7 Paketen leer

Nicht aus den Urteilen uebernommen, selbst gefunden und belegt. Der Index definiert einen Manifest-Wortschatz:

```js
Z.160  var WORTE = {};
Z.161  function W(){ return (WORTE && WORTE[PaketCore.gruppeVonParty(PaketCore.party())]) || {}; }
Z.164  function G(){ return W().gruppe || (WORTE.mittel&&WORTE.mittel.gruppe) || 'Gruppe'; }
Z.165  function H(){ return W().held   || (WORTE.mittel&&WORTE.mittel.held)   || ''; }
```

Gemessen ueber alle gebauten Pakete (`grep -c 'G()'`, Definition eingerechnet):

| Paket | `WORTE` | `G()`-Treffer | echte Aufrufstellen |
|---|---|---|---|
| **ritter** | befuellt (`{"klein":{"gruppe":"Ritterbande","held":…}}`) | 17 | **15** (u.a. Z.424 Cover, Z.520 Einkauf, Z.668 Spiele, Z.693/696 Urkunde, Z.721 Tischkarte) |
| baustelle, piraten, dino, feuerwehr, meerjungfrau, prinzessin | `var WORTE = {};` | 3 | **0** (2 Definitionen + 1 Kommentar) |

Fuer baustelle ist die Manifest-Spalte also durchgaengig leer, und jedes Motto-Wort („Bautrupp", „auf dem Bauplatz", „Baustellen-Schicht", „Junior-Baumeister") ist ein Literal im Blatt. Das ist heute **kein Defekt** — die Woerter sind pro Paket ohnehin motto-spezifisch generiert — aber die Schicht ist **unerklaerte Doppel-Mechanik**: ritter personalisiert ueber Daten, die sechs anderen ueber Generator-Literale. Zwei Wahrheiten fuer dieselbe Sache. → Backlog #9.

---

## 2. Feld-Register `data/motto`

Status-Werte: **LEBENDIG dossier** (Paket druckt es) · **LEBENDIG planerseite** (nur Web/SEO oder Wizard-Runtime) · **TOT** (kein lebender Renderer im Repo).
Jedes Feld aus Abschnitt A des mechanischen Reports ist enthalten.

### 2.1 Struktur- und Steuerfelder

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `variants` | LEBENDIG dossier | `variant()` speist jedes Blatt | — |
| `id` (variants) | LEBENDIG dossier | Z.659 `_vr[(variant()||{}).id]` — waehlt Rollen-Level | — |
| `_meta`, `version`, `lastUpdated`, `qualityNote` | TOT (per Konvention) | Bau-Metadaten; in `STRIP_KEYS` (`_src/gen-elite-bundle.cjs:19`) | nichts — als Metadaten erklaert |
| `ageGroup` (top) | LEBENDIG dossier | Report: GELESEN | — |
| `motto` (top) | **TOT** | Alle Konsumenten leiten den Slug anders ab: Generator aus der Dateinamen-Schleife (`_src/generate-age-pages.py:653-656`), Paket aus der HTML-Konfig (`paket/core/paket-core.js:343` `const mid = CFG().id||''` → :414 `fetch('/data/motto/'+mid+'-'+grp+'.json')`), Wizard aus `id+Gruppe` (`kindergeburtstag.html:1786`). Alle `get('motto')`-Treffer im Repo sind `URLSearchParams`. `check-ungelesene-felder.py:24` fuehrt `motto` in `INTERN` → wird nie gemeldet | loeschen ODER Erklaerung ins Doc (Redundanz zum Dateinamen) |
| `title` (top) | **TOT** | `_src/generate-age-pages.py:499-501` weist zu und praefixt das Emoji — die Variable wird **nie ausgegeben**. Der Seitentitel kommt aus :508 `page_title = f"{brand['display']}-Kindergeburtstag {age_range} Jahre — 3 Party-Konzepte"`. Der JSON-Wert `'🚧 Baustelle-Kindergeburtstag — 3–5 Jahre'` kommt in der gebauten Seite **0×** vor. Report meldet „title GELESEN" — Namenstreffer auf `preparationWeeks…items[].title` / `savingsTip.title` / `cakeRecipe.tips[].title` | loeschen (tote Zuweisung) ODER `page_title` daraus ableiten — **Entscheidung noetig, aktuell unerklaert** |
| `ageRange` (top, z.B. `[3,5]`) | **TOT** | Repo-weit **kein** Property-Zugriff. Generator nutzt Hardcode `AGE_RANGE_DISPLAY = {"klein":"3-5",…}` (`_src/generate-age-pages.py:30`, selbst gelesen) + :497. `consolidate-age-pages.js:155` leitet aus Dateinamen ab. `INTERN`-gelistet → nie gemeldet | rendern (Generator auf `d['ageRange']` umstellen, dann ist `AGE_RANGE_DISPLAY` erklaert) ODER loeschen |

### 2.2 `sosScenarios` — der groesste Falsch-Positiv-Block des Reports

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `sosScenarios` | LEBENDIG dossier | Z.685-698 | — |
| **Alle Szenario-Keys**: `mehr_kinder_als_erwartet`, `weniger_kinder_als_erwartet`, `schrauben_verloren`, `schubkarre_kippt`, `kind_isst_sand`, `kind_will_nicht_mitmachen`, `kuchen_misslungen`, `baustellen_besuch_abgesagt`, `brücke_bricht_zu_schnell`, `sabotage_zu_kompliziert`, … | **LEBENDIG dossier** | Selbst gelesen, Z.686-689: `const sos=DATA.sosScenarios||{};` / `const keys=Object.keys(sos);` / `if(!keys.length) return '';` / `const cards=keys.map(k=>{ const s=sos[k];` — **Object.keys-Loop, KEINE Key-Whitelist**. Zweiter Konsument `_src/generate-age-pages.py:368` `for key, s in sos.items():`, dritter `_src/kindergeburtstag.jsx:772` `Object.entries(sosScenarios)`. Report Z.19/22/60/61/63/69/101/104/105/127 = reines Grep-Artefakt | **nichts** — kein Befund |
| `sosScenarios.regen` | LEBENDIG dossier (doppelt) | Z.450 `const sos=DATA.sosScenarios||{}; const regen=sos.regen;` + Z.469 Plan-B-Anriss auf Seite 2, der woertlich auf „Details in Teil III (SOS-Karte)" verweist → Doppeldruck ist **erklaert**. Existiert in 44/45 Dateien; `regen?`-Guard deckt die 45. | nichts |
| `icon`, `label`, `headline`, `steps`, `fallback` | LEBENDIG dossier | Z.690-695 | — |
| **`tone`** (Werte: praktisch, ruhig, einfuehlsam, humorvoll, klar, deeskalierend, diplomatisch, entlastend, motivierend, pragmatisch, sanft, ernst, ernsthaft, bestimmt, beruhigend, locker) | **TOT** | `grep 'tone'` ueber `index.html`, `template.html`, `paket-core.js` = **0 Treffer** (selbst verifiziert, Exit-Code 1). `generate-age-pages.py:365` nennt `tone` **nur im Docstring**, der Funktionskoerper :370-374 holt es nicht. `kindergeburtstag.jsx` SOSButton rendert es nicht. Einziger echter Leser ist ein Generierungs-Validator (`_dev/content-loop/runs/05-einhorn-mittel-phase-b/_validate_and_merge.py:54`). Beweis der Folgenlosigkeit: **24 SOS-Eintraege haben gar kein `tone`** und rendern vollstaendig | **Erklaerung ins Doc**: `tone` als reines Autoren-/Generator-Steuerfeld deklarieren, das nie druckt — sonst loeschen. Als Druckfeld ist es Karteileiche |

### 2.3 `preparationWeeks` — Whitelist statt Loop

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `preparationWeeks` | LEBENDIG dossier | Z.473-504 | — |
| `minus6Weeks`, `minus4Weeks`, `minus2Weeks`, `minus1Week`, `minus2Days`, `minus1Day`, `dayOf` | **LEBENDIG dossier** | Selbst gelesen, Z.476 `const order=[['minus6Weeks','6 Wochen vorher'],…]` + Z.486 `const present=order.filter(([k])=>pw[k]);`. Konsum ist **String-Literal-Liste, nicht Object.keys** → Report-Kandidat falsch. **Eigener Datenabgleich ueber alle 45 Dateien**: exakt diese 7 Keys existieren (`minus1Week` 45×, `minus1Day` 45×, `dayOf` 45×, `minus4Weeks` 44×, `minus2Weeks` 40×, `minus2Days` 39×, `minus6Weeks` 1× in piraten-gross) — **kein Key ausserhalb der Whitelist**, heute faellt nichts durch | **Linter-Regel**: Whitelist gegen die real vorkommenden Keys pruefen — ein neuer Key wuerde heute **still verschwinden**, ohne FAIL |
| `items[]`, `icon`, `title`, `detail` | LEBENDIG dossier | Z.491 `liOf` — mit String/Objekt-Drift-Guard (Gate-M4-Familie) | — |
| **`preparationWeeks.<key>.headline`** | **LEBENDIG planerseite** | Im Paket TOT: `grep 'headline'` liefert nur Z.469 (`regen.headline`) und Z.691 (`s.headline`) — **kein `pw[k].headline`**; Z.496-499 druckt stattdessen das Code-Literal `esc(label)`. Planerseite lebendig: `kindergeburtstag.html:2015` `head: ph.headline \|\| ''`. **Eigene Messung ueber 45 Dateien**: 28× `minus4Weeks` „4 Wochen vorher — Grundgerüst stellen", 21× `minus2Weeks` „2 Wochen vorher — Logistik festziehen", 17× `dayOf` „Tag X — durchziehen, atmen, feiern" — im Paket steht davon nur der generische Vorderteil | **rendern** (`pw[k].headline` mit Fallback aufs Label) oder Erklaerung, warum das bezahlte Blatt die kuerzere Fassung druckt |

### 2.4 `ageAdjustN` — Regex-Konsum, komplett entlastet

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `ageAdjust3`, `ageAdjust5`, `ageAdjust6`, `ageAdjust8` | **LEBENDIG dossier** | Selbst gelesen, `paket/core/paket-core.js:49-52`: `const stufen = Object.keys(game\|\|{}).map(k => { const m = /^ageAdjust(\d+)$/.exec(k); return m ? {n:+m[1], k} : null; })` — **liest jeden Suffix per Regex, nicht per Namensliste**. Aufruf im gebauten Index Z.596 `const _p = PaketCore.ageAdjustFor(g, _a);`, Druck Z.619. Absicht im Kommentar Z.46 belegt: „Statt drei Suffixe fest zu verdrahten, lesen wir jetzt alle ageAdjustN". Daten: 12/14 klein-Spiele mit `ageAdjust3`, 14/14 mit `ageAdjust5`, 14/14 mittel + 16/16 gross mit `ageAdjust6`/`ageAdjust8` | **nichts** — Kandidaten entlastet; der Linter kennt das bereits (`check-ungelesene-felder.py:30` `DYNAMISCH = re.compile(r'^ageAdjust\d+$')`) |
| `ageAdjust*` in **baustelle-gross** | **LUECKE** (32 geschrieben, 0 gedruckt) | Guard Z.617-619 selbst gelesen: `const _sp = _grp==='klein' ? [3,5] : (_grp==='gross' ? [9,12] : [6,8]); const _passt = (_st!=null && _st>=_sp[0] && _st<=_sp[1]);`. `baustelle-gross.json` enthaelt **ausschliesslich** `ageAdjust6` (16×) + `ageAdjust8` (16×) — keine Stufe 9-12. Folge: der gross-Kaeufer bekommt auf **keiner** Spielkarte eine Altersanpassung. Der Guard-Kommentar (Z.608-615) waehlt bewusst „Fehlender Rat ist besser als falscher" → Luecke **erklaert, aber unbehoben**. Mechanisch erfasst: `check-altersstaffeln.py` FAILt („74 Spiele mit Staffeln ausserhalb ihres Altersbands, in 45 Dateien") | **rendern** — `ageAdjust9`/`ageAdjust12` fuer gross autoren. Linter-Stufe 26 existiert und ist rot; **sie zu gruenen ist der Fix**, nicht eine neue Regel |
| `ageAdjust4` (baustelle-klein, „🃏 Werkzeug-Memory") | **LUECKE (Planerseite)** | Paket trifft ihn (`paket-core.js:56`). Planerseite **kann nicht**: `kindergeburtstag.html:1987` `const map={'3-5':['ageAdjust3','ageAdjust5'],'6-8':['ageAdjust6','ageAdjust8'],'9-12':['ageAdjust8','ageAdjust6']};` — `ageAdjust4` steht in keiner Liste. Das Spiel hat nur `ageAdjust5` + `ageAdjust4`. Ein 4-Jaehriger bekommt still die 5-Jahres-Fassung. Exakt die Fehlerklasse, die Gate B6.7 im Paket beseitigt hat (`paket-core.js:42-48`: „328 von 1329 Anpassungen unsichtbar") — im Paket behoben, in der Planerseite nicht | **Linter-Regel** gegen hartkodierte `ageAdjustN`-Namenslisten ausserhalb `ageAdjustFor()`; Fix: `eliteAgeAdjust()` auf dieselbe Regex umstellen |

### 2.5 Spielkarten-Felder

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `games`, `name`, `material`, `prepText`, `kern`, `safetyRule`, `indoorTip`, `outdoorTip`, `duration` | LEBENDIG dossier | Z.590-625 | — |
| `quizCards`, `frage`, `optionen`, `antwort`, `note` | LEBENDIG dossier | Z.335-345 `quizBlock(g)` | — |
| **`quizCards[].kategorie`** | **TOT** | Einziger Quizkarten-Renderer ist der Paket-Index, und `quizBlock` (Z.335-345) konsumiert nur `frage`/`optionen`/`antwort`/`note`. Sonst niemand: `kindergeburtstag.html` rendert **gar keine** Quiz-Karten; `generate-age-pages.py` nennt weder `quizCards` noch `kategorie`. Datenmenge: baustelle-mittel 36 + gross 75 Karten — **111/111 tragen eine kategorie** (Werte: Werkzeug-Erkennen, Bau-Berufe, Materialien). Linter: `check-ungelesene-felder.py:56-59` `BEKANNT` | **rendern** — 75 ungruppierte Karten auf dem gross-Blatt sind eine Wand; die Kategorie ist die fertige Zwischenueberschrift. Alternativ loeschen + BEKANNT-Zeile streichen |
| `alibiTabelle`, `verdaechtige`, `spuren`, `aufloesung`, `alibi_behauptung`, `alibi_problem`, `motiv`, `spuren_zuordnung`, `foto_hinweis`, `fundort`, `passt_zu` | LEBENDIG dossier | Report: GELESEN (Detektiv-Mechanik) | — |
| **`effort`** | **TOT** | `grep -rn "effort" paket/` = **0** (selbst verifiziert). `kindergeburtstag.html` = 0. Alters-Planerseiten = 0; `generate-age-pages.py` nennt es nicht. Einziger Konsument ist **toter Code**: `_src/kindergeburtstag.jsx:1038/1103` — und `AUDIT.md:9` sagt: „`js/kindergeburtstag.js` / `_src/kindergeburtstag.jsx` (React, 3,8 MB) = TOT — von KEINER HTML-Seite geladen"; `ls js/` bestaetigt (nur baby/birthday-project/einschulung/index/motto-data/theme-registry/wizard.js). Daten vollstaendig gepflegt (gross: `{'hoch':4,'leicht':6,'mittel':6}`). **Freispruch im Linter ist STALE**: `check-ungelesene-felder.py:21-25` begruendet `effort` als „Steuerdaten" (Constraint-Solver-Filter) — dieser Zweck existiert live nirgends mehr | **rendern** („Aufwand: hoch" als Badge — der Kaeufer plant Vorbereitungszeit) oder loeschen. Die `INTERN`-Zeile muss so oder so neu begruendet oder gestrichen werden |
| **`whyItWorks`** | **LEBENDIG planerseite** | `grep -rn "whyItWorks" paket/` = **0** (selbst verifiziert). Planerseite druckt es: `kindergeburtstag.html:1947` `${g.whyItWorks?`<p class="egame__why"><b>${esc(g.whyItWorksTitle\|\|'Warum es wirkt')}:</b> ${esc(g.whyItWorks)}</p>`:''}` (selbst gelesen), aufgerufen je Spielzeile aus `renderPlanList`. Daten: **alle 14 klein + 14 mittel + 16 gross** baustelle-Spiele tragen non-empty `whyItWorks`. Dieselbe Inversion, die Gate B1.2/M6.4 fuer `optOutNote`/`rolesList` beseitigt hat (Z.672-675: „stand in 39 Dateien und wurde vom PAKET nie gedruckt — die kostenlose Planer-Seite zeigt es laengst"). Nebenbefund: `check-ungelesene-felder.py:58` BEKANNT („kein Renderer liest es") ist **unwahr** | **rendern** — 44 fertige Begruendungstexte stehen dem Gratis-Planer zur Verfuegung und dem bezahlten Blatt nicht. Zusaetzlich BEKANNT-Zeile bereinigen |
| **`whyItWorksTitle`** | **LEBENDIG planerseite** | Identischer Pfad, als Ueberschrift mit Fallback (`kindergeburtstag.html:1947`). Paket 0 Treffer. Ohne `whyItWorks` wertlos | **rendern** — gemeinsam mit `whyItWorks` entscheiden; getrennt ergibt keinen Sinn |

### 2.6 Einkaufsliste & Kosten

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `shoppingList`, `emoji`, `label`, `priceEur`, `url`, `hasAffiliate`, `food`, `giveaways`, `decoration`, `timeWindow`, `costContext` | LEBENDIG dossier | Z.507-543 (selbst gelesen) | — |
| **`estimatedCostEur`** | **LEBENDIG planerseite + Gate** | Zwei Konsumenten: Druck `generate-age-pages.py:213` → :276 `<span class="cost-value">~{esc(cost)} €</span>` (live: `cost-value">~130 €` / `~207 €` / `~361 €`); **deterministisches Gate** `validate-all.sh:387-390` Stufe 11 („estimatedCostEur weicht von der gedruckten Countdown-Zahl ab"). Im Paket ungedruckt — die Summe wird dort **gerechnet** (Z.515-531) | **nichts** — Erklaerung ins Doc: im Paket bewusst ungedruckt, Feld dient Planerseite + Linter Stufe 11 |
| **`savingsTip`** (+ `title`, `body`) | **LEBENDIG planerseite** | `kindergeburtstag.html:2064` liest es korrekt als `{title, body}`. Im Paket 0 Treffer. **Aber Generator-Bug, s. §4** | nichts fuers Feld — Generator-Fix noetig |
| **`category`** (shoppingList) | **TOT** | Kein Renderer gruppiert danach. Live-Planer rechnet ueber das **Label**: `kindergeburtstag.html:1998` `/^Optional:/.test(l)`; das Paket genauso (Z.522, selbst gelesen). Einzige `category`-Gruppierung: `_src/kindergeburtstag.jsx:851` `CAT_MAP` — **toter Pfad** (Build-Ziel `js/kindergeburtstag.js` existiert nicht, `netlify.toml` hat `publish="."` ohne build-Command). Uebrig bleibt ein Build-WARN auf ein nicht ausgeliefertes Bundle | **Bolle-Entscheid**: rendern (Gruppierung Pflicht/Sinnvoll/Hab-ich-vielleicht) oder loeschen; solange es bleibt, Linter-Regel auf kanonische Werte statt WARN im toten Build |
| **`categoryReasoning`** | **LEBENDIG planerseite** | `kindergeburtstag.html:2051` `const reason = s.categoryReasoning ? ` title="${escA(s.categoryReasoning)}"` : '';`. Daten: 10/10 Items in baustelle-mittel/standard. **Ehrliche Einschraenkung**: landet ausschliesslich im `title`-Attribut — auf Touch unsichtbar, im Paket gar nicht | nichts (erklaert) — optional als Kaufbegruendung in die gedruckte Liste heben |

### 2.7 Ritual (`signatureRitual`)

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `signatureRitual`, `introText`, `setupSteps`, `rolesList`, `emoji`, `name`, `function`, `materialNote`, `optOutNote` | LEBENDIG dossier | Z.637-679 (selbst gelesen) | — |
| `stempelHeft` | **FREIGESPROCHEN** | Voll verdrahtet **und als Gate begruendet**: Z.278 `if(!(rit && rit.stempelHeft)) return '';`, Z.286 Typ-Guard, Z.290 Druck. Identisch in allen 7 Paketen + `template.html:224/232`. Begruendung im Code (Z.271-277): ohne Gate erzaehlte die geteilte Vorlage allen Mottos ein Heft, das weder Einkaufsliste noch Ritual liefert. Daten stimmen: baustelle-mittel hat es, klein/gross nicht — und dort druckt der Block korrekt gar nicht | **nichts** — Musterbeispiel einer erklaerten Konstante |
| **`subtitle`** | **LEBENDIG planerseite** | `generate-age-pages.py:164` → :194 `<h3>{esc(subtitle)}</h3>`; live: `kindergeburtstag/baustelle-6-8-jahre.html:240` „Eroeffnet die Party mit Schwur, endet mit Lizenz-Uebergabe". Im Paket ungedruckt (Z.637-679 liest `name`/`introText`/`setupSteps`/`rolesList`/`materialNote`/`optOutNote`) | **rendern** — der Satz ist genau die Ein-Zeilen-Klammer (Anfang/Ende), die die Ritualkarte heute nur als Tag `Anfang & Ende` behauptet |
| **`roleCountByVariant`** (+ `minimal`, `standard`, `wow`) | **TOT** | Kein Renderer im Repo. Treffer nur in Schreibseite (`_src/patch-phaseb-welle5.py`/`welle6.py`), Datenkopie (`_bundle.js`) und `_vorschau_test.html` (von keiner Seite referenziert). Nur 3/45 Dateien tragen es | **Linter-Regel** — s. §4, das Feld widerspricht der gedruckten Wahrheit |
| **`printables`**, **`printablesNote`**, **`fields`**, **`format`** | **TOT** | Kein Renderer. Treffer nur in `_src/patch-phaseb-welle8.py`, `_bundle.js`, `_vorschau_test.html`. In `paket/`, `kindergeburtstag.html`, `generate-age-pages.py`: **0**. Nur 3/45 Dateien. **Blindes Rendern druckte ein Versprechen ins Leere**: baustelle-gross `printablesNote` = „6 Druckvorlagen als PDF-Pack bei machsleicht.de/downloads/baustelle-printables.pdf (in Vorbereitung — manuell auf Anfrage)" — **`ls downloads` → No such file** (selbst verifiziert) | **loeschen** (oder erst die PDFs bauen, dann rendern) — auf keinen Fall ungeprueft rendern, sonst druckt das bezahlte Paket eine 404-URL |

### 2.8 Kuchen

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `cakeRecipe`, `intro`, `steps`, `n`, `content`, `meta`, `aufwand`, `kosten`, `allergiker` | LEBENDIG dossier | Z.557-566 (selbst gelesen) | — |
| **`cakeRecipe.tips[]`** (+ `title`, `body`) | **LEBENDIG planerseite** | Nur `generate-age-pages.py:309` → :340 `<li><strong>{esc(t.get("title",""))}</strong> {esc(t.get("body",""))}</li>`. Im Paket **nicht**: `shDecoCake` liest `c.steps`, `c.meta.*`, `c.intro` — `c.tips` kommt nicht vor (selbst gelesen). Auch der Live-Planer laesst tips liegen (`kindergeburtstag.html:2066` nur `intro`+`steps`). Inhalt vorhanden, z.B. mittel: „🎂 Keine Lust zu backen? — Baecker-Schoko-Kuchen mit Kruemel-Sand bestreuen … Spart 75 Min" | **rendern** — der Nicht-Backen-Ausweg ist die groesste Entlastung des Kuchenblatts und fehlt ausgerechnet auf dem bezahlten Blatt |

### 2.9 SEO-/Planerseiten-Cluster (im Paket bewusst abwesend)

Sammelbeleg: Grep ueber `paket-core.js`, `baustelle/index.html`, `template.html` nach `faq|parentTips|ageInsight|whyMottoFits|introParagraph|metaDescription|invitationTemplate|ageRange|educationalValue` ergibt **genau einen Treffer, und der ist ein Kommentar** (`paket-core.js:288`). Diese Felder sind nicht tot — sie sind **woanders zuhause**.

| Feld | Status | Beleg | Konsequenz |
|---|---|---|---|
| `faq`, `q`, `a` | **LEBENDIG planerseite** | Zwei Renderer: `generate-age-pages.py:356-357` + FAQPage-JSON-LD :125-126; Wizard-Runtime `kindergeburtstag.html:2068`. Gebaute Seite enthaelt „Brauche ich echtes Werkzeug?" und `FAQPage` | nichts (Arbeitsteilung ins Doc) |
| `parentTips`, `structured`, `topic`, `detail` | **LEBENDIG planerseite** | `generate-age-pages.py:446` unter `<h2>👨‍👩‍👧 Eltern-Tipps</h2>`; zusaetzlich `kindergeburtstag.html:2067` | nichts |
| `parentTips.educationalValue` | **LEBENDIG planerseite** | Einziger Renderer `generate-age-pages.py:442` + :447 „📚 Lernwert:". Wizard liest **nur** `.structured` | Erklaerung ins Doc (bzw. Kandidat fuers Eltern-Blatt) |
| `ageInsight`, `headline`, `whyMottoFitsHeadline`, `whyMottoFits`, `traits`, `topic`, `detail` | **LEBENDIG planerseite** | `generate-age-pages.py:146-157` (h3 + Absaetze) und :150-153 (`trait-row`). Live: „Warum 3–5-Jährige bei jedem Bagger stehen bleiben", `trait-topic">Aufmerksamkeit/Sensorisches Lernen/Rollen-Identität/Bewegung/Sicherheit` → widerlegt „topic NIE GELESEN" | Erklaerung ins Doc |
| `introParagraph` | **LEBENDIG planerseite** | `generate-age-pages.py:503` + :591 unter der h1; aus dem Planer-Bundle ausdruecklich gestrippt (`gen-elite-bundle.cjs:19`) | Erklaerung ins Doc |
| `metaDescription` | **LEBENDIG planerseite** | Vier Druckstellen: :502, :551 `<meta name="description">`, :553/:563 og+twitter, :111 HowTo-JSON-LD | nichts |
| `invitationTemplate` | **LEBENDIG planerseite** | `generate-age-pages.py:540` → :395-398 `<h2>💌 Einladungs-Text (zum Kopieren)</h2>`. Im Paket 0 Treffer | **rendern** — Kandidat fuers Einladungs-Blatt (deckt sich mit PBI #105 „Einladung konvergieren") |
| `bonusGames`, `items`, `intro`, `name`, `duration`, `minAge`, `material` | **LEBENDIG planerseite** | `generate-age-pages.py:543` + Renderer ab :453. Live: `baustelle-6-8-jahre.html:673 <h3>🃏 Werkzeug-Pantomime</h3>` (9 Seiten). Im Paket 0 Treffer; aus dem Bundle gestrippt (`gen-elite-bundle.cjs:20`) → Report-Kandidat widerlegt | nichts (Paket) + Erklaerung: SEO-/Planerseiten-Content |
| **`bonusGames.items[].anleitung`** | **TOT** | Der einzige Renderer liest diesen Namen nie: `generate-age-pages.py:463` `rules = g.get("rules", g.get("description",""))` und :470 `steps = g.get("steps", g.get("howToPlay",[]))` (selbst gelesen — **kein** `anleitung`). Daten: baustelle-mittel `items[0].anleitung` = „Kind zieht Karte und stellt Werkzeug pantomimisch dar…". Folge live (`baustelle-6-8-jahre.html:673-678`): Titel, Tags, Material — **danach zwei leere Zeilen**. Gedruckt wird ein Spiel ohne Spielregel | **rendern** (`anleitung` in die rules-Fallback-Kette) + Linter-Regel: jedes bonusGames-Item braucht einen gelesenen Anleitungs-Key |
| **`bonusGames.items[].indoor` / `.outdoor`** | **LUECKE** | Kommen in `render_bonus_games` (:453-489) **nicht** vor — der Report meldet sie faelschlich als GELESEN (Namenskollision mit `games[].indoor` auf der Spielkarte, Z.585). Auf der Bonus-Karte fehlt die Ort-Angabe | rendern (Ort-Tag analog Spielkarte) — sonst loeschen |
| `bonusGames.items[].loudness` | LEBENDIG planerseite | **IST** gelesen: `generate-age-pages.py:468` → Tag `<span class="game-tag">mittel</span>` live in `baustelle-6-8-jahre.html:674` | — |

---

## 3. PARTY-Luecken — was der Plan liefert und das Paket ignoriert

Alle Greps gegen `paket/` selbst nachgezaehlt: **0 Treffer** fuer `notes`, `paypalMe`, `vielleicht`, `hasPhoto`, `hasGamePhoto`, `askAllergies`, `askPickup`, `claimedBy`, `respondedAt`.

| # | Was der Plan/Worker liefert | Was das Paket tut | Nutzen-Idee |
|---|---|---|---|
| P1 | **`PARTY.notes`** — persoenliche Nachricht. `kindergeburtstag.html:2928` `notes: (state.partyMessage \|\| '')`; Partyseite rendert sie (`party-worker.js:2399`). Das Wizard-Feld heisst woertlich „z.B. Wir freuen uns riesig auf euch — kommt gern verkleidet!" (`kindergeburtstag.html:840`) | ignoriert sie komplett | **rendern** auf Einladungskarte, Eltern-Handzettel und in den WhatsApp-Text. Der Gastgeber hat den Satz einmal geschrieben — genau den Satz, der auf die gedruckte Einladung gehoert |
| P2 | **Gast-Status `vielleicht`** — dritter RSVP-Zustand. `party-worker.js:574` akzeptiert `["ja","nein","vielleicht"]`, :2377 zeigt den Zaehler | kennt nur zwei: `paket-core.js:239` `confirmedGuests()` filtert `status==='ja'` (selbst gelesen); `:225-226` teilt Invites nur in `open`/`declined`. Folge: ein „vielleicht"-Kind bekommt Einladung, aber **keine Urkunde, kein Tischkaertchen, keine Zeile auf dem Kuechen-Zettel** | **rendern** — dritte Statusgruppe im Kuechen-Zettel + Blanko-Reserve aus der vielleicht-Anzahl statt fest 1 (Z.721) |
| P3 | **Allergien von „vielleicht"-Gaesten** — Partyseite zaehlt **statusunabhaengig**: `party-worker.js:2356` `party.guests.filter(g=>g.allergies)` → Warnkarte :2381 | Paket filtert vorher auf `ja` (Z.178), dieselbe Quelle speist `allergieTausch()` (Z.196). Ein Kind mit „vielleicht" + Erdnussallergie steht auf dem **Bildschirm** in der Warnkarte und auf **keinem** gedruckten Blatt | **rendern** — Allergie-Zeile und Kuchen-Austausch auf ja+vielleicht ziehen (Status am Namen markieren). **Der Druck darf nicht weniger wissen als der Bildschirm.** Beruehrt PBI #108 (Art.-9-Check) |
| P4 | **`wishes[].claimedBy` / `claimedCount` / `claimedAmountTotal` / `isFull`** — Worker liefert den Reservierungsstand an beide Sichten (`party-worker.js:402`), Partyseite druckt „Gesamt: X€" / „Noch offen" (:2467-2468) | druckt nur Titel+Preis+`sharedGift`-Tag (Z.762, selbst gelesen). Der Kuehlschrank-Zettel zeigt Wuensche, die laengst reserviert sind, ohne Kennzeichnung | **rendern** — je Wunsch „reserviert / noch offen"; bei `sharedGift` zusaetzlich der zusammengelegte Betrag |
| P5 | **`PARTY.paypalMe`** — im Vertrag (`WORKER-CONTRACT.md` Z.30), sanitisiert gespeichert (`party-worker.js:363`), der Partyseite gezeigt (:2472) | 0 Treffer — obwohl **auf demselben Blatt** ein `<span class="tag gold">Gemeinschaftsgeschenk</span>` gedruckt wird (Z.762) und fertige WhatsApp-Texte an die Gast-Eltern geliefert werden | **rendern** — bei mindestens einem `sharedGift` den paypal.me-Handle in den WhatsApp-Text und auf den Kuechen-Zettel; sonst ist das Gemeinschaftsgeschenk auf Papier **ohne Zahlweg** |
| P6 | **`party.hasPhoto` / `photo:<id>`** — Worker speichert (`party-worker.js:377-379`), fuehrt das Flag mit (:375) und liefert das Bild **oeffentlich** aus (`/api/ogimg/<id>`, :768ff, `Access-Control-Allow-Origin: *`) | `grep '<img\|background-image\|url(data:'` ueber Index+Core = **0**. Cover, Urkunde, Poster, Bautagebuch sind reine SVG/Text-Blaetter | **rendern** — Flag ist ohne Zusatz-Request da, Endpoint CORS-offen: Foto auf Cover, Urkunden-Medaillon, Countdown-Poster. **Das Keepsake-Blatt ohne das Kind darauf ist der teuerste ungenutzte Datensatz im Paket.** Deckt sich mit PBI #106 (Print-Wuerde) |
| P7 | **`party.hasGamePhoto` / `invphoto:<id>`** — der Wizard laesst den Gastgeber das Foto **zuschneiden** (`kindergeburtstag.html:2929 photoRound`), Auslieferung oeffentlich (`party-worker.js:748`) | 0 Treffer. Das Paket druckt dieselbe Party komplett ohne dieses Bild | **rendern** — bereits quadratisch zugeschnittenes Motiv fuer Tischkaertchen/Einladungskarte; sonst Erklaerung, warum das gedruckte Produkt das einzige Foto der Party nicht kennt |
| P8 | **`askAllergies` / `askPickup`** — echte Schalter auf der Creator-Seite (`party-worker.js:1313-1314`), steuern das RSVP-Formular (:1993) und den Autosend (:2042) | Paket druckt beides **bedingungslos**: Spaltenkopf `Allergien / Essen` + `Abholung` (Z.767), „✅ Zu-/Absage & Allergien" (Z.929), „bitte Allergien angeben" (Z.934). **Praezisierung nach eigener Pruefung**: der Wizard-Payload hardcodet `askAllergies: true, askPickup: true` (`kindergeburtstag.html:2928`) — fuer Wizard-Partys stimmt das Papier also immer. Die Luecke beisst nur bei Partys, die ueber die **Creator-Seite** oder per PUT abgeschaltet werden. Real, aber schmaler als zunaechst formuliert | **rendern** — Spalten und Aufforderungstexte an die Flags binden. Der Kommentar Z.798-799 unterstellt heute pauschal „Planer-Partys fragen immer ab" — das ist nur wegen des Payload-Hardcodes wahr |
| P9 | **`invites[].role` + `PASS_TITLES`** — Worker fuehrt je Motto Rolle+Mission (`party-worker.js:1081`) und einen kanonischen Pass-Titel (:1088 `baustelle:"BAUSTELLEN-AUSWEIS"`) | Rolle nur auf Einladung (Z.811) und Tischkaertchen (Z.735). Die **Urkunde ist fuer alle Kinder identisch** (Z.705 `Junior-Baumeister`), die Einladung spricht generisch von „dein Party-Pass wartet" (Z.803) | **rendern** — Rolle + Mission auf die Urkunde (personalisierter Abschluss statt Serienbrief), Pass-Titel aus `PASS_TITLES` statt Generikum |
| P10 | **`guest.respondedAt` / `party.created`** — Worker stempelt jede Antwort (`party-worker.js:578`) und jede Party (:364) | 0 Treffer; **kein Blatt traegt ein Datum**. Das Paket weist selbst zum Neudrucken an (Z.787 „Sagt doch noch jemand zu, drucke dieses Blatt einfach neu.") — ohne Datum kann der Gastgeber zwei Ausdrucke nicht unterscheiden | **rendern** — „Stand: <letzte RSVP>" in den `sheetfoot` der gaesteabhaengigen Blaetter |
| P11 | **Plan-Gaestezahl `state.guests`** — der Plan kennt sie und rechnet damit (`kindergeburtstag.html:1390`, :1999 skaliert die Einkaufsliste, :1767) | **Kommt im Payload nicht vor** (selbst gelesen, `kindergeburtstag.html:2913-2930`). Paket faellt auf RSVPs zurueck: Z.432 `confirmedGuests().length \|\| (parseInt(PARTY.guestCount,10)\|\|0)`, in `shShopping` sogar **ohne** guestCount-Fallback (Z.509). Am Kauftag (0 Zusagen) druckt das bezahlte Produkt „Bautrupp rückt noch an", waehrend die **kostenlose** Planerseite die echte 6 rechnet | **Datenfeld einfuehren** — `plannedGuests` in `/api/create` + PUT (Whitelist 2..20 analog `ageGroup`/`ambition`), im Paket als Fallback **vor** `guestCount`; danach greift `kostenKontext()` mit der geplanten statt der Musterzahl |
| P12 | **Plan-Spielauswahl `state.games` / `eliteOff` / `customEntries`** — der Plan fuehrt sie (`kindergeburtstag.html:1397-1399`; `eliteOff` = im Elite-Plan abgewaehlte Spielnamen) | **Keines davon im Payload** (:2913-2930, selbst gelesen). Das Paket baut den Ablauf aus der Variante neu — die Abwahl des Gastgebers ueberquert die Grenze nicht. Wer im Planer ein Spiel abwaehlt, findet es im bezahlten Paket wieder | **Datenfeld einfuehren** — `eliteOff` (Namensliste) mitschicken und in `buildTimeline`/`shGameCards` respektieren. Bis dahin: Erklaerung, dass das Paket die vollstaendige Variante druckt |

---

## 4. Hardcode-Register

| Hardcode | Urteil | Fundstelle |
|---|---|---|
| `foot('Seite 9 · SOS')` und die Nachbarn `'Seite 4'` / `'Seite 5'` | **ANGEKLAGT** → Seitenzahl beim Zusammenbau **zaehlen** statt ins Blatt tippen. Selbst nachgerechnet an `render()` (Z.977-995): physische Folge ist Cover(1), Timeline(2), Countdown(3), **Poster(4, Footer ohne Nummer)**, Shopping(**5**, druckt „Seite 4"), DecoCake(**6**, druckt „Seite 5"), GameCards(7), Stations(8), SOS(**9**). Shopping/DecoCake sind seit dem Poster **um eins verschoben**. „Seite 9" stimmt nur zufaellig und nur, solange `shStations` ein Blatt liefert — Z.836 `if(!SCHATZ) return '';` und Z.839 `if(!sts.length) return '';` lassen es entfallen, dann ist SOS physisch Blatt 8 und druckt trotzdem „Seite 9" | Z.698, Z.543, Z.567; gleiches Literal `template.html:614` → **alle 7 Pakete** |
| **order-Labels ueberstimmen die Daten** (`['minus4Weeks','4 Wochen vorher']`) | **ANGEKLAGT** → Label aus `pw[k].headline` ziehen. Selbst gemessen: **5 Dateien** tragen unter `minus4Weeks` die headline „3 Wochen vorher" — `prinzessin-{klein,mittel,gross}.json`, `superheld-{mittel,gross}.json`. Das Paket ignoriert sie und druckt „4 Wochen vorher" plus ein aus `offs.minus4Weeks:-28` gerechnetes Datum. **Derselbe Datenblock erscheint im gekauften Paket als „4 Wochen vorher / bis <Datum−28>" und auf der kostenlosen Planerseite als „3 Wochen vorher"** (`kindergeburtstag.html:2015` `ph.headline`). Betrifft live gebaute Pakete (`paket/prinzessin/index.html` vorhanden); superheld hat noch kein Paket | `paket/prinzessin/index.html:468`, `offs` :473; entspricht baustelle Z.476/481 |
| **`roleCountByVariant` vs. `abVariante`-Ableitung** | **ANGEKLAGT** → Linter-Regel; die Regel ist mechanisch moeglich und **schlaegt sofort an**. Selbst nachgerechnet mit `_vr={minimal:0,standard:1,wow:2}` (die Ableitungsvorschrift steht im Produkt: Z.660 / `paket/ritter/index.html:647`): **baustelle-gross** deklariert {4,6,8} → Ableitung 4/6/8 **OK**. **ritter-gross** deklariert {4,6,8}, aber **keine** der 8 Rollen traegt ein `abVariante` → Ableitung 8/8/8 **FAIL**. **pferde-gross** deklariert {5,7,8}, ebenfalls 0 `abVariante` → 8/8/8 **FAIL**. Produktschaden ist real und bezahlt: `paket/ritter/index.html` druckt in **jeder** Variante alle 8 Rollenzettel, waehrend das Datenfeld fuer „minimal" 4 behauptet — zwei Wahrheiten, und die gedruckte gewinnt unbemerkt | Z.660; `data/motto/{ritter,pferde,baustelle}-gross.json` |
| **`esc(saving)` auf ein dict** | **ANGEKLAGT** → analog :340 title/body getrennt rendern. Selbst verifiziert: `savingsTip` ist in den Daten ein dict `{title, body}` (alle 3 Varianten baustelle-mittel), `esc()` ist :35 `html.escape(str(s), quote=True)`, und :279 ist die **einzige** Verarbeitungsstelle. Ausgabe waere Python-Syntax im Fliesstext. Die **ausgelieferte** Seite zeigt dagegen sauberes `<strong>💡 Warum kostet „Minimal" 130 €?</strong> Lizenz-Heft-System…` und `grep -c "&#x27;title&#x27;"` = **0** → die 9 Seiten sind mit **aelterem** Code erzeugt; **der naechste Generatorlauf bricht sie**. Gleiche Fehlerfamilie wie die im File selbst dokumentierten Faelle (:338 „{'title','body'}-dicts — roh escaped stand sonst Python-Syntax im Druck") — dort schon gefixt, hier uebersehen | `_src/generate-age-pages.py:215/279` |
| **Generator-Quelle `_src/elite-motto-data`** | **ANGEKLAGT** → Quelle auf `data/motto` zwingen. Selbst gelesen: `generate-age-pages.py:18` `JSON_DIR = ROOT / "_src" / "elite-motto-data"` — aber `_src/build.sh:13` sagt woertlich: „ACHTUNG: Quelle ist data/motto/, NICHT mehr _src/elite-motto-data/*.json (deprecated, stale)." Drift messbar in `faq`, `parentTips`, `ageInsight`. Live-Folge: die Seite druckt 2× die alte Sicherheitsformulierung „Mindestens 5 cm — nicht verschluckbar", waehrend die aktuelle Fassung „Nichts verwenden, was komplett durch eine Klopapierrolle passt…" lautet — „Klopapierrolle" kommt auf der Seite **0×** vor | `_src/generate-age-pages.py:18` vs. `_src/build.sh:13` |
| **`ageInsight` als String statt dict** (6 Dateien) | **ANGEKLAGT** → Schema angleichen. Selbst nachgezaehlt: `prinzessin-{klein,mittel,gross}` und `superheld-{klein,mittel,gross}` tragen `ageInsight` als String. Der einzige Renderer erwartet ein dict (`generate-age-pages.py:146` `ageInsight.get("headline","")`). Heute harmlos, weil beide Mottos nicht in `MOTTO_BRAND` stehen — **sobald der Generator erweitert wird, ist das ein AttributeError** | `data/motto/{prinzessin,superheld}-*.json` |
| **`ageInsight` fehlt in `STRIP_KEYS`** | **ANGEKLAGT** (mild) → aufnehmen. `gen-elite-bundle.cjs:16-21` begruendet die Liste („Top-Level-Keys, die der Planer-Runtime NICHT liest … werden nur auf den statischen Motto-HTML-Seiten gerendert"). `ageInsight` erfuellt **exakt dieselbe** Bedingung, fehlt aber und wandert als Ballast ins Bundle | `_src/gen-elite-bundle.cjs:16-21` |
| **`MOTTO_BRAND` deckt 3 von 15 Mottos** | **ANGEKLAGT** → Generator auf alle 15 ziehen. Selbst gelesen: `MOTTO_BRAND = {"pferde":…, "ritter":…, "baustelle":…}` — mehr nicht. Messung ueber alle 45 Dateien gegen die zugehoerige Planerseite: `educationalValue` nur **18/45** auffindbar, `invitationTemplate` 27/45, `traits[].detail` 29/45, `metaDescription` 31/45, `introParagraph` 34/45, `faq[0].q` 37/45. baustelle/pferde/ritter sind 7/7 gruen — **genau die drei Generator-Mottos** | `_src/generate-age-pages.py:22-26` |
| **Feld-Linter scannt kein `.py`** | **ANGEKLAGT** → auf `**/*.py` erweitern. `check-ungelesene-felder.py:48` `for muster in ('**/*.js', '**/*.html', '**/*.jsx'):` — Python-Renderer werden nie gelesen. Deshalb steht der halbe SEO-Cluster faelschlich in `BEKANNT` (:58-62 `'educationalValue', … 'metaDescription', 'introParagraph', 'invitationTemplate'`) als „kein Renderer liest es", obwohl der Generator sie alle druckt. **Gegenlaeufiger Fehler**: `_vorschau_test.html:341` bettet Voll-JSON ein und erzeugt Namenstreffer, weshalb der Lauf `0 Feldnamen ungelesen` meldet. **Die Regel irrt in beide Richtungen** | `_dev/scripts/check-ungelesene-felder.py:48/58-62` |
| `s.label \|\| k` (SOS-Kartenueberschrift) | **LUECKE** — heute toter Zweig: ueber alle 45 Dateien haben **0** SOS-Eintraege kein `label`. Er ist aber die einzige Absicherung, und **kein Test erzwingt `label`**; schlaegt er zu, steht `brücke_bricht_zu_schnell` als Ueberschrift auf dem bezahlten Blatt | Z.690 |
| Nicht-ASCII-Key `brücke_bricht_zu_schnell` | **FREIGESPROCHEN** — der Key wird nie gedruckt: `shSOS` nutzt ihn nur als Lookup (Z.689) und rendert `s.label` (vorhanden). Keine Whitelist, kein Key-Literal-Vergleich. Der Umlaut ist kosmetisch, kein Renderdefekt | `data/motto/baustelle-gross.json` |
| Alters-Baender `_sp = [3,5]/[6,8]/[9,12]` | **FREIGESPROCHEN** — Produktentscheidung mit Begruendung im Code (Z.608-615): „Fehlender Rat ist besser als falscher — und die Luecke wird sichtbar, statt sich als Fehlinformation zu tarnen." Verhindert, dass ein 10-Jaehriger „NICHT fuer 8" liest | Z.617 |
| `stempelHeft`-Gate | **FREIGESPROCHEN** — begruendet Z.271-277: ohne Gate erzaehlte die geteilte Vorlage allen Mottos ein Heft ohne Material und Ritual | Z.278/286 |
| Gerechnete Einkaufs-Summe (`Σ`) | **FREIGESPROCHEN** — vorbildlich: begruendet Z.515-521 („47 handgepflegte Gesamtpreise waren von ihren Listen abgewichen … eine Zahl, die jemand pflegen muss, geht irgendwann falsch"). Diese Zahl **kann** nicht abweichen | Z.522-531 |
| 6 Tischkaertchen / 4 Handzettel / 14 Poster-Naechte / 5 Logbuch-Fragen / 1 Blanko-Urkunde | **FREIGESPROCHEN** — Blatt-Formate (Schneidegitter, Aushang), nicht Gaeste-Wahrheiten. Der Leerzustand deckt sein eigenes Versprechen (Z.718 „zwei Blanko-Urkunden" ↔ Z.721+722) | Z.727, Z.943, Z.909, Z.896, Z.721-722 |
| `'Startzeit 14:00 ist angenommen'` | **FREIGESPROCHEN** — explizite Ehrlichkeits-Ansage statt fabriziertem Fakt; wird nur gedruckt, wenn `tl.timeAssumed` | Z.453 |
| `'Junior-Baumeister'` / `'Offizielle Richtfest-Urkunde'` | **FREIGESPROCHEN als Motto-Text**, aber **ANGEKLAGT als Personalisierung** — s. P9: `invites[].role` + `PASS_TITLES` liegen bereit, die Urkunde bleibt fuer alle Kinder identisch | Z.705 |
| **`WORTE={}` + ungenutzte `G()`/`H()`** in 6 von 7 Paketen | **ANGEKLAGT** (neu, nicht aus den Urteilen) — selbst gemessen: nur ritter hat ein befuelltes Manifest und **15 echte `G()`-Aufrufe**; baustelle, piraten, dino, feuerwehr, meerjungfrau, prinzessin haben `var WORTE = {};` und **0 echte Aufrufstellen** (nur die 2 Definitionen). Zwei parallele Personalisierungs-Mechaniken fuer dasselbe Ziel; die leere Schicht ist unerklaertes Geruest | Z.160-165; `paket/ritter/index.html:424/520/668/693/696/721` |

---

## 5. Konsequenzen-Backlog

Priorisiert nach *bezahltem Schaden zuerst*, dann *Datenverlust*, dann *Hygiene*. Jede Position ist auf ≤ 1 Session geschnitten.

**Rang A — das gedruckte Produkt sagt heute etwas Falsches**

1. **Seitenzahlen zaehlen statt tippen.** `foot()` bekommt die Nummer beim Zusammenbau in `render()`; `'Seite N'`-Literale raus (Z.543, 567, 698 + `template.html:614`, alle 7 Pakete). Hilfsweise Linter-Regel gegen `Seite \d` in `foot()`. → deckt sich mit **#110 (gedruckte Zahlen aus Datenlaengen)**.
2. **Countdown-Label aus `pw[k].headline`** mit Fallback aufs order-Label — beendet den Widerspruch „4 Wochen vorher" (Paket) vs. „3 Wochen vorher" (Planerseite) in 5 Dateien. Danach entscheiden: die 5 Dateien auf `minus3Weeks` umschluesseln **oder** die headline korrigieren. → **#110**, **#76 (Zahlen-/Konsistenz-Sweep)**.
3. **`savingsTip`-Renderer fixen** (`generate-age-pages.py:279` → title/body getrennt wie :340) **+ Linter-Regel „kein `esc()` auf dict/list im Generator"**. Ohne diesen Fix bricht der naechste Generatorlauf 9 ausgelieferte Seiten.
4. **Generator-Quelle auf `data/motto` umstellen** (`generate-age-pages.py:18`) + Linter-Regel, die die Quelle erzwingt, **+ 9 Seiten neu rendern**. Loest zugleich die veraltete Sicherheitsformulierung („5 cm" statt Klopapierrollen-Check). → **GSC-Nachlauf zu #98**; nach dem Rendern **GSC-Re-Submit nicht vergessen**, falls sich `sitemap.xml`/`lastmod` aendert.

**Rang B — geschriebene Inhalte, die der Kaeufer bezahlt und nicht bekommt**

5. **`ageAdjust9`/`ageAdjust12` fuer baustelle-gross autoren** — heute bekommt der gross-Kaeufer auf **keiner** Spielkarte eine Altersanpassung. Linter-Stufe 26 (`check-altersstaffeln.py`) ist bereits rot; **sie zu gruenen ist der Fix**. (Gesamtbild: 74 Spiele in 45 Dateien betroffen — hier nur baustelle-gross schneiden.)
6. **`cakeRecipe.tips[]` + `signatureRitual.subtitle` ins Paket rendern** — der „Keine Lust zu backen?"-Ausweg ist die groesste Entlastung des Kuchenblatts; `subtitle` ist die Anfang/Ende-Klammer, die die Ritualkarte heute nur behauptet. Beides klein und additiv.
7. **`whyItWorks` + `whyItWorksTitle` ins Paket** (44 fertige Begruendungstexte je Motto, heute nur im Gratis-Planer) — **oder** dokumentierte Entscheidung „Planer-only". Danach BEKANNT-Zeile im Linter bereinigen. Gleiche Inversion wie Gate B1.2/M6.4.
8. **`bonusGames.items[].anleitung` in die rules-Fallback-Kette** (`generate-age-pages.py:463`) — heute steht auf 9 Live-Seiten ein Bonus-Spiel **ohne Spielregel**. `indoor`/`outdoor` als Ort-Tag gleich mitnehmen.

**Rang C — PARTY-Daten, die die Grenze nicht ueberqueren**

9. **`PARTY.notes` drucken** (Einladung, Handzettel, WhatsApp-Text) — kleinster Aufwand, groesster Waermegewinn: der Gastgeber hat den Satz schon geschrieben. → **#105 (Einladung konvergieren)**.
10. **Status `vielleicht` einbauen** — dritte Statusgruppe im Kuechen-Zettel, **Allergien auf ja+vielleicht ziehen** (der Druck darf nicht weniger wissen als der Bildschirm), Blanko-Reserve aus der vielleicht-Anzahl. → beruehrt **#108 (Art.-9-Check)**: beim Anfassen gleich die Datenminimierung mitentscheiden.
11. **Foto aufs Papier** — `hasPhoto` → Cover/Urkunde/Poster, `hasGamePhoto` → Tischkaertchen/Einladung. Flags liegen im Party-JSON, Endpoints sind CORS-offen, kein neuer Request noetig. → **#106 (Print-Wuerde-Welle)**, **#107 (Heldenkarten-Set)**.
12. **`plannedGuests` als Datenfeld einfuehren** (`/api/create` + PUT, Whitelist 2..20) und im Paket **vor** `guestCount` als Fallback nutzen — beendet „Bautrupp rückt noch an" am Kauftag, waehrend der Gratis-Planer mit der echten Zahl rechnet.
13. **Personalisierung nachziehen**: `invites[].role` + Mission auf die Urkunde, `PASS_TITLES` statt „dein Party-Pass wartet"; **„Stand: <letzte RSVP>"** in den `sheetfoot` der gaesteabhaengigen Blaetter. → **#110**.
14. **`paypalMe` + Wunsch-Reservierungsstand** (`claimedCount`/`isFull`/`claimedAmountTotal`) auf den Kuechen-Zettel und in den Gast-Eltern-Text — sonst ist das Gemeinschaftsgeschenk auf Papier ohne Zahlweg.
15. **`askAllergies`/`askPickup` binden** + den Wizard-Payload-Hardcode (`true`/`true`) als Produktentscheidung dokumentieren oder aufloesen.
16. **`eliteOff` ueber die Grenze bringen** — die Spiel-Abwahl des Gastgebers respektieren, statt die volle Variante zu drucken.

**Rang D — Register sauber halten (Doktrin-Infrastruktur)**

17. **Feld-Linter reparieren**: `**/*.py` mitscannen, daten-einbettende HTML (`_vorschau_test.html`) ausschliessen, danach die `BEKANNT`-Liste aufloesen und die stale `INTERN`-Begruendung fuer `effort` neu fassen oder streichen. **Ohne diesen Schritt bleibt jedes kuenftige Feld-Urteil unzuverlaessig** — die Regel irrt derzeit in beide Richtungen. → **#110**.
18. **`roleCountByVariant`-Regel** implementieren: `roleCountByVariant[v] == |{r ∈ rolesList : LV(r.abVariante) ≤ LV(v)}|`. 2 von 3 Dateien FAILen sofort → `abVariante` in ritter-gross/pferde-gross nachziehen **oder** das Feld dort loeschen.
19. **`preparationWeeks`-Whitelist-Regel** (neuer Key wuerde heute still verschwinden) + **`ageAdjustN`-Namenslisten-Verbot** ausserhalb `ageAdjustFor()` (Fix: `eliteAgeAdjust()` in `kindergeburtstag.html:1987` auf die Regex umstellen) + **`label` als Pflichtfeld** je `sosScenarios`-Eintrag (danach ist der `s.label||k`-Fallback erklaert oder entfernbar) + **Feldtyp-Regel** (`ageInsight` muss dict sein — 6 Dateien FAILen).
20. **Loeschen/Erklaeren-Runde** (eine Session, rein additiv-negativ): `printables`/`printablesNote`/`fields`/`format` **loeschen** (sonst druckt das bezahlte Paket eine 404-URL — `downloads/` existiert nicht); `motto`, `title`, `ageRange` entscheiden (loeschen vs. Generator daraus speisen); `tone` als Autoren-Steuerfeld deklarieren; `category` Bolle-Entscheid; `ageInsight` in `STRIP_KEYS`; `quizCards[].kategorie` als Zwischenueberschrift rendern oder streichen.
21. **`MOTTO_BRAND` auf alle 15 Mottos ziehen** (Quelle `data/motto`) — heute sind 12 Mottos unversorgte Handkopien (`educationalValue` nur 18/45 auffindbar). Erst **nach** Backlog-Punkt 4 und der `ageInsight`-Typangleichung, sonst AttributeError.
22. **Manifest-Schicht vereinheitlichen** — entweder `WORTE` fuer alle Pakete befuellen (dann ist `G()`/`H()` erklaert) oder die tote Schicht aus den 6 Paketen und aus `template.html` entfernen. Zwei Personalisierungs-Mechaniken nebeneinander sind genau der Zustand, den die Doktrin verbietet.


---

## Anhang A — Nachzuegler-Sweep 1: party-worker.js + Einladungs-Pipeline

### worker

| Gegenstand | Urteil | Konsequenz |
|---|---|---|
| KV-Key `party:<id>` — der einzige Party-Datensatz (JSON, TTL = Partydatum + 14 Tage) | **LEBENDIG** | Konsumenten: Worker-Serve (guestPageFull/editorView), Public-GET (Paket via paket-core.js:385 fetch). Alles andere haengt daran. |
| Vollstaendiges Feld-Schema von `party:` (Stand Code, NICHT Stand WORKER-CONTRACT.md) | **LEBENDIG** | 27 Felder. `guestCount` ist KEIN gespeichertes Feld, sondern ein Public-GET-Derivat (Z.404) — mit edit-Token fehlt es in der Antwort komplett. |
| `invites[]` Sub-Schema: `{t (Token, 16 Z.), n (Vorname, max 50), role, stamps[]}` | **LEBENDIG** | t/n/role werden von Partyseite (Party-Pass Z.1929-1946), Editor (Z.2549 `const INVITES=`) und Paket (paket-core.js:212 splitInvites, roleLabel) gelesen. |
| `guests[]` Sub-Schema: `{name, status, allergies, pickupTime, pickupPerson, respondedAt, inv?}` | **LEBENDIG** | Konsumenten: Editor-Gaesteliste (Z.2428-2436), Paket-Kuechenzettel (paket/piraten/index.html:743-759), Paket-Abholplan (Z.230-233), splitInvites-Absagenfilter. |
| `guests[].respondedAt` — Antwort-Zeitstempel | **TOT** | Wird bei jeder RSVP geschrieben und nie gelesen. Entweder Nachzuegler-Sortierung/"zuletzt geantwortet"-Anzeige bauen oder Feld streichen (KV-Value-Bytes je Gast). |
| `invites[].stamps[]` — Sammel-Stempel des Party-Passes | **TOT** | Ein angefangenes Feature ohne jede Mechanik. Der Pass rendert nur Rolle/Mission/Status (Z.1938-1941). Entweder Stempel-Mechanik bauen oder Feld raus. |
| `party.created` — Erstellungszeitpunkt | **TOT** | Nie gerendert, nie fuer TTL/Analytics genutzt (TTL rechnet aus `date`). Tot, aber harmlos. |
| `party.ref` — virale Attribution Gast→Host | **TOT** | Kette ist ZWEIFACH gebrochen: (1) der einzige ref-Link zeigt auf die Creator-Seite — party-worker.js:2031 `<a href="https://party.machsleicht.de/?ref=${esc(id)}">` — und deren Prefill-Liste Z.1660 `["childName","age","motto","mottoEmoji","mottoColor","mottoId" |
| `party.email` — Host-E-Mail (einziger Kanal zum Gastgeber) | **TOT** | Geschrieben, DSGVO-relevant gespeichert, nie verwendet. Siehe naechster Eintrag — daran haengt ein gebrochenes Produktversprechen. |
| Versprechen "Erinnerung 7 Tage vor der Party" ohne jede Maschinerie | **ANGEKLAGT** | Drei Mal versprochen, null Mal implementiert. Die beiden dafuer noetigen Felder (`party.email`, `party.date`) liegen im KV und werden von niemandem gelesen. Entweder Cron-Trigger + Reminder-Job bauen (Daten sind vollstaendig da) oder das Versprechen aus allen  |
| `party.ageGroup` — die Altersgruppen-ENTSCHEIDUNG des Wizards (3-5/6-8/9-12) | **LUECKE** | Die Partyseite ignoriert ageGroup komplett. Konkrete Folge: die Spiel-URL (party-worker.js:1752) haengt `&age=` NUR an, wenn `party.age` gesetzt ist — und `age` ist auf dem Wizard-Normalweg leer (kindergeburtstag.html:2913 "#26: nur das ECHTE Alter an die Part |
| `party.mottoColor` — im Creator waehlbare Akzentfarbe | **LUECKE** | Die vom Host gewaehlte Farbe faerbt ausschliesslich die Editor-Ansicht, die nur er selbst sieht. Die Gastseite nimmt immer die THEMES-Palette (Z.133-150). Ein Custom-Motto mit eigener Farbe rendert Gaesten das DEFAULT_THEME-Orange. Entweder mottoColor in die G |
| `party.notes` (Persoenliche Nachricht des Hosts) | **LUECKE** | Der einzige frei formulierte, persoenliche Satz der Eltern erscheint auf der Partyseite, aber auf keinem einzigen gedruckten Blatt des bezahlten Pakets. Kandidat fuer Deckblatt/Einladungskarte des Pakets. |
| `party.paypalMe` — Gemeinschaftsgeschenk-Kasse | **LUECKE** | Wer ueber den Hauptfunnel (Planer) kommt, kann PayPal NIE setzen — weder beim Anlegen noch nachtraeglich. Der Gemeinschaftsgeschenk-Button ist auf dem Hauptweg strukturell tot. Auch das Paket liest paypalMe nicht (0 Treffer unter paket/). |
| `party.hasPhoto` + KV-Key `photo:<id>` — das Hero-/OG-Foto | **LUECKE** | Jede ueber den Planer erzeugte Party hat `hasPhoto=false` — die WhatsApp-Vorschau des Gaeste-Links zeigt das generische og-home.png statt des Kindes, obwohl das gecroppte Foto als `invphoto:<id>` im selben KV liegt. Das ist der teuerste virale Moment des Produ |
| KV-Key `photoRound:<id>` — Altbestand des kaputten PATCH-Kontrakts | **TOT** | Wird nur noch geloescht, nie geschrieben oder gelesen. Korrekt aufgeraeumt — als Aufraeum-Pfad erklaert und damit gerechtfertigt, aber irgendwann streichbar (Altbestand laeuft per TTL ohnehin aus). |
| KV-Key `invphoto:<id>` (Party-Spielfoto) + `party.hasGamePhoto` | **LEBENDIG** | Alive. TTL wird via refreshPhotoTtl (Z.295-300) mitgezogen. |
| KV-Keys `extimg:<id>` + `extimgtok:<id>` — Standalone-/e/-Produkt (90 Tage) | **LEBENDIG** | Erklaerte Trennung, sauber. Der Loeschpfad (DELETE /api/invphoto/:id, Z.716-724) haengt am `extimgtok` aus der Upload-Response — einladung/erstellen/index.html speichert diesen deleteToken allerdings nirgends persistent (nur `fid` wird weiterverwendet, Z.590-5 |
| KV-Key `wl:<ts><rand>` — Produkt-Warteliste (E-Mail + Produkt, TTL 12 Monate) | **TOT** | Schreib-only. Die Warteliste ist nur ueber das Cloudflare-KV-Dashboard (Key-Prefix-Listing) einsehbar — es gibt keinen Weg, sie zu exportieren, ohne per Hand zu klicken. Bei "Start-Info" an die Warteliste wird das der Engpass. |
| KV-Key `consent:<sha256(email)>` — DSGVO-Art.-7-Nachweis, 3 Jahre | **FREIGESPROCHEN** | Erklaerte Konstante mit Rechtszweck; Auslesen ueber das Dashboard ist bei einer Beweispflicht der richtige Weg. Kein Handlungsbedarf. |
| KV-Keys `rl:create:` / `rl:guestwrite:` / `rl:wl:` / `rl:invphoto:` / `rl:editmail:` / `rl:editmail:party:` — Rate-Limits | **FREIGESPROCHEN** | Jede Drossel traegt Grenzwert-Begruendung im Kommentar (z.B. Z.558 "W8-11: 30/h drosselte legitime Klassen hinter Schul-WLAN/CGNAT"). Erklaerte Konstanten. |
| KV-Keys `font:baloo2\|dmsans\|fraunces` — selbst gehostete Fonts | **LEBENDIG** | Alle drei tatsaechlich verlinkt (Gastseite = Baloo 2, Creator/Editor/404 = Fraunces). Kein toter Font. |
| KV-Key `doi:<token>` + `party.doiToken` — Double-Opt-In | **LEBENDIG** | Alive und DSGVO-sauber verkettet. |
| `party.ambition` (minimal/standard/wow) — die Wizard-Variantenwahl | **LEBENDIG** | Nur das Paket liest es — die Partyseite ignoriert es. Fuer die Partyseite ist das vertretbar (sie zeigt kein Programm), aber siehe naechster Eintrag: es gibt keinen Korrekturweg. |
| Kein Schreibweg im Editor fuer ageGroup / ambition / gameId / askAllergies / askPickup / paypalMe | **LUECKE** | Der Server kann mehr als jede UI. Wer im Wizard versehentlich die falsche Altersgruppe oder Ambition waehlt, kann sie NIRGENDS mehr korrigieren — das bezahlte Paket bleibt dauerhaft falsch (falsche Sicherheitszeilen bei klein, s. paket-core.js:83-88). Sechs Fe |
| Status `vielleicht` — gespeichert, im Paket unsichtbar | **LUECKE** | SICHERHEITSRELEVANT: Ein Kind mit Status 'vielleicht' bekommt eine gedruckte Einladungskarte (open-Zweig), erscheint aber NICHT im Kuechen-Zettel — paket/piraten/index.html:744 `const ja=guests.filter(g=>g&&g.status==='ja');`. Seine gemeldete Allergie steht au |
| Allergie-Detailtiefe: 200 Zeichen Freitext, im Paket nur als Rohstring durchgereicht | **LEBENDIG** | Alive auf drei Sinks (Editor-Warnkasten Z.2438-2441, Kuechen-Zettel, Einkaufs-/Menue-Hinweis). Aber: reiner Freitext — es gibt keine Struktur (Allergen-Tag vs. Praeferenz), deshalb die Negations-Regex als Kruecke. Das ist auch der Anknuepfungspunkt fuer Ticket |
| `party.mottoId` fuer die Paket-Auswahl ignoriert — das Paket kennt sein Motto nur aus dem Dateipfad | **LUECKE** | Aendert der Host das Motto im Partyseiten-Editor (party-worker.js:433 `if(ROLE_CATALOG[_cand] && _cand!==party.mottoId) body.mottoId=_cand;` — Theme, Rollen und Pass ziehen mit), bleibt das bereits verlinkte Paket auf dem alten Motto stehen. Partyseite und bez |
| Einladungs-Studio hat NULL Datenverbindung zum Worker | **LUECKE** | Das Studio kennt weder `invites[]` (Name+Rolle+Token) noch `guests[]` noch `mottoId` aus dem KV — nur den Planer-Cache auf DIESEM Geraet. Genau das blockiert Ticket #105 ("personalisierte Gast-Variante mit Name/Rolle/QR im Rand"): die Daten liegen im Worker, d |
| `ort=` und `tel=` in der Spiel-URL sind fest verdrahtete Leerstrings | **ANGEKLAGT** | `ort=` ist ERKLAERT (Z.1748-1749 "Adress-Gating: ort NICHT in die Spiel-URL (sichtbar im iframe-src = Leak vor Zusage)", Gegenstueck in spiele/core/core.js:183) → insoweit freigesprochen. `tel=` traegt KEINE Erklaerung und hat auch keine Datenquelle: das party |
| Redaktionsdaten hart im Worker: MOTTO_EMOJI, MOTTO_COLORS, THEMES, ROLE_CATALOG, ROLE_DEFAULT, PASS_TITLES, GAME_META | **ANGEKLAGT** | Diese Kataloge existieren ein ZWEITES Mal von Hand gepflegt im Paket: paket/piraten/index.html:70-77 — Kommentar `/* Rollen — EXAKT der ROLE_CATALOG der Partyseite (party-worker.js), inkl. Mission. */` und dann die Rollen woertlich abgeschrieben. Genau die "ha |
| Spiel-Katalog doppelt gepflegt: GAME_CATALOG/IND_GAMES im Worker vs. GAME_WL in der Netlify-Function | **ANGEKLAGT** | Zwei Wahrheiten fuer "welches Spiel gibt es". Der Kill-Switch des Workers (Z.1042 "status:\"go\" ... Kill-Switch: status aendern -> Galerie blendet aus") greift auf dem /e/-Pfad NICHT — serve-invite.mjs kennt kein status-Feld. Ein abgeschaltetes Spiel bleibt u |
| Shop-Listen dreifach: AFFILIATE_RULES, SAFE_WISH_DOMAINS, shopLabel — plus eine vierte Kopie im Client-JS | **ANGEKLAGT** | Ein neuer Partner muss an vier Stellen eingetragen werden, sonst: Link erlaubt aber ohne Affiliate-Tag, oder Tag gesetzt aber Label "ansehen". Kandidat fuer eine gemeinsame Konstante. |
| Custom-Motto faellt beim Spiel hart auf "piraten" | **ANGEKLAGT** | Der Fallback ist ERKLAERT (Ticket #32: lieber ein Spiel als keins), aber die Folge steht nirgends im UI: eine "Zirkus"-Party bekommt ein Piraten-Spiel, waehrend Theme (getTheme→DEFAULT_THEME) und Pass (ROLE_DEFAULT) neutral bleiben. Drei verschiedene Fallback- |
| `guestCount` ist kein KV-Feld, sondern nur im Public-GET vorhanden — mit Token fehlt es | **LEBENDIG** | Das Paket faengt das korrekt ab: paket/piraten/index.html:424 `const g=confirmedGuests().length \|\| (parseInt(PARTY.guestCount,10)\|\|0);` und Z.746 nutzt guestCount nur `if(!HASTOKEN)`. Sauber — aber eine unausgesprochene Asymmetrie der API, die in WORKER-CO |
| `wishes[].claimedBy`-Betraege (Gemeinschaftsgeschenk-Summen) im Paket ignoriert | **LUECKE** | Der Kuechen-Zettel druckt die Wunschliste OHNE Reservierungsstand — der Host sieht auf Papier nicht, was schon vergeben ist, obwohl der Datensatz (mit Token) claimedBy vollstaendig liefert. Eine Spalte "reserviert von" waere reine Render-Arbeit. |
| `_dev/docs/WORKER-CONTRACT.md` beschreibt einen Worker, den es nicht mehr gibt | **ANGEKLAGT** | Alle Zeilennummern der Endpoint-Tabelle (Z.67-72) sind falsch, und 11 der 19 Routen fehlen ganz: /api/party/:id/invites, /api/waitlist, POST+DELETE /api/invphoto, /api/invimg/:id, /api/ogimg/:id, /api/newsletter-confirm, /fonts/:name.woff2, GET / (Creator), GE |
| Konsumenten-Matrix Kurzfassung — wer liest was | **LEBENDIG** | Kein einziger Konsument liest: `ref`, `created`, `email`, `respondedAt`, `stamps`. Nur ein Konsument liest: `ageGroup`+`ambition` (nur Paket), `mottoColor`+`notes`+`paypalMe`+`askAllergies/askPickup` (nur Partyseite). |
| Der Wizard-Payload und der Creator-Payload sind zwei verschiedene Vertraege auf denselben Endpoint | **LUECKE** | Je nach Eingangstuer entsteht eine strukturell andere Party: Wizard-Partys haben nie OG-Foto und nie PayPal, Creator-Partys nie Altersgruppe, nie Ambition, nie Gaesteliste — und bekommen damit im Paket still `ageGroup(null)→'mittel'` und `VARIANT='standard'` ( |

### einladung

| Gegenstand | Urteil | Konsequenz |
|---|---|---|
| data/motto/*.json Feld `invitationTemplate` (45 Dateien) — wird von NIEMANDEM gelesen (weder Studio, noch Vorlagen-Seiten, noch Planer, noch Worker) | **TOT** | Entweder verdrahten (natuerlicher Ort: die 15 x /einladung/<motto>/vorlagen/-Seiten und der Studio-Einladungstext) oder aus allen 45 JSONs loeschen. Aktuell ist es redaktionell gepflegter Text (Welle-11-Konsistenz-Sweep hat ihn noch 2026 angefasst, s. baustell |
| Studio-Motto-Katalog `themes` (15 Eintraege mit mission/badge/message) ist vollstaendig im HTML hardcodiert — dritte, unabhaengige Motto-Wahrheit neben data/mot | **ANGEKLAGT** | Das Studio kann per Definition nicht mit dem Motto-Datenstand mitwandern. Aendert jemand das Piraten-Wording in den 3 JSONs, druckt die Einladung weiter die alte Formel. Fuer #105 (EIN Studio-Design als Quelle fuer WhatsApp+Druck+Gastkarte) muss dieser Katalog |
| Kartenformat 420x746 px hart verdrahtet (Export = 3x -> 1260x2238). Kein Druckformat, kein @page, kein @media print | **LUECKE** | Genau der Kern von Ticket #105. Ein 10x15-Druckweg ist nicht 'ein zweiter Export', sondern verlangt entweder eine parametrisierte Karten-Geometrie oder ein Passepartout-Wrapping (1260x2238 in 1200x1800 einbetten). Solange 420/746 als Literal an ~20 Stellen ste |
| Plan-Feld `state.crew` / `state.crewText` (Vornamen der eingeladenen Kinder) wird vom Studio NIE gelesen | **LUECKE** | Schliesst #105 ohnehin (personalisierte Gast-Variante). Bis dahin: das Studio-PNG ist immer generisch, obwohl der Planer die Namen bereits kennt — die Personalisierung existiert nur im Paket-Druck (paket/ritter shInvitations) und auf der Partyseite. |
| Gast-ROLLEN existieren im Planer-State ueberhaupt nicht — sie entstehen erst serverseitig in `party.invites[].role`. Das Studio kann sie folglich auch nicht ign | **LUECKE** | Fuer #105 (Name/Rolle/QR im Rand der Gast-Karte) muss das Studio erst an die Worker-Quelle `party.invites` angebunden werden — der localStorage-Plan reicht dafuer nicht. Das ist ein Architektur-Schritt, keine Layout-Erweiterung, und gehoert explizit in den #10 |
| Plan-Feld `state.partyMessage` (persoenliche Nachricht an die Gaeste) wird ignoriert — das Studio ueberschreibt den Einladungstext aktiv mit seiner hardcodierte | **ANGEKLAGT** | Namenskollision mit Fallhoehe: zwei Felder heissen partyMessage, tragen Unterschiedliches, und das Studio-Feld gewinnt beim Druck. Wer im Planer eine persoenliche Nachricht schreibt, findet sie auf der Partyseite wieder, aber nie auf der Einladung. Umbenennen  |
| Plan-Felder `state.invite.title` / `state.invite.body` / `state.invite.deadline` werden ignoriert — der Planer BAUT einen vollstaendigen Einladungstext samt abg | **ANGEKLAGT** | Zwei parallele Einladungstexte im selben Funnel: einer im Planer-Preview, einer im Studio. Die Mutter sieht im Schritt davor einen Text, der im Studio verschwindet. Fuer #105 ('EIN Studio-Design') muss genau eine der beiden Textquellen sterben. |
| Die Antwort-Frist (`state.invite.deadline`) erscheint NIRGENDS auf der Studio-Karte — obwohl der Planer sie datumsabgeleitet berechnet und alle 7 hardcodierten  | **LUECKE** | Die zentrale Handlungsaufforderung einer Einladung ('bis wann zusagen') fehlt im Produkt, das die Einladung erzeugt. Fuer den QR-/RSVP-Fall ist das teilweise ersetzt, fuer den Druckweg (#105) nicht — dort gibt es keinen Rueckkanal ausser dem gedruckten Datum. |
| Hardcodierter Orts-Fallback 'Bei uns zuhause' wird als echter Ort GEDRUCKT, wenn `state.adresse` leer ist | **ANGEKLAGT** | Der Gast liest eine Ortsangabe, die niemand eingegeben hat und die bei einer Hallen-/Bauernhof-Party schlicht falsch ist. Entweder Zeile weglassen (wie bei leerer Uhrzeit geloest) oder als sichtbaren Platzhalter kennzeichnen. Auf dem Fremdgeraet-Pfad feuert di |
| URL-Handoff (`plannerStateFromUrl`) transportiert weder `adresse` noch `partyseite.url` — auf einem fremden Geraet druckt das Studio deshalb zwingend 'Bei uns z | **ANGEKLAGT** | Der auf dem Handy geteilte Autopilot-Link erzeugt eine Einladung OHNE Rueckkanal — genau das Gegenteil des Produktversprechens. Fix ist billig (party-URL + Ort in die Query, URL-Laenge unkritisch, im Gegensatz zum bewusst ausgeklammerten Foto). Bis dahin ist d |
| Datenschutz-Asymmetrie: `state.adresse` ist auf der Partyseite bewusst zusage-gegated, wird vom Studio aber ungefiltert ins frei teilbare PNG gedruckt | **ANGEKLAGT** | Entweder ist das Worker-Gating ueberzogen oder der PNG-Druck ist zu offen — beides gleichzeitig kann nicht die gewollte Regel sein. Muss vor #105 entschieden werden, denn die personalisierte Gast-Karte macht die Adresse pro Kind druckbar. Beruehrt Ticket #108  |
| Demo-Defaults 'Matti' / 'Hamburg' / '20. Juli 2026' / '14:00 Uhr' sind hardcodiert und koennen mitgedruckt werden, sobald der Nutzer irgendein anderes Feld anfa | **ANGEKLAGT** | Wer nur den Namen aendert und Datum/Ort uebersieht, teilt eine Einladung zum 20. Juli 2026 in Hamburg. Robuster waere ein Feld-fuer-Feld-Check (jedes Feld, das noch exakt dem Default entspricht, blockiert oder wird sichtbar markiert) statt eines globalen _touc |
| Geratenes Alter wird gedruckt: fehlt `exactAge` UND `age`, setzt das Studio stumm 6 — die Karte behauptet dann 'WIRD 6!' | **ANGEKLAGT** | Gleiche Fehlerklasse wie das bereits geschlossene Ticket #26 (Partyseite zeigt geratenes Alter), hier nur auf dem gedruckten Artefakt. Der Code kennt bereits den ehrlichen Zweig ('{n} FEIERT!' bei leerem Alter) — der Gruppen-Mittelwert und die 6 sollten auf ge |
| Plan-Feld `state.guests` (Gaesteanzahl) wird vom Studio nicht konsumiert | **LUECKE** | Fuer die WhatsApp-Karte irrelevant (bewusst), fuer #105 relevant: die Anzahl bestimmt die Auflage der personalisierten Gast-Karten und die Zahl der Blanko-Reserven. Der Paket-Druck loest das heute ueber `party.invites`, nicht ueber guests — beim Zusammenlegen  |
| Plan-Felder `state.games` / `eliteOff` / `customEntries` / `state.location` werden ignoriert — die Einladung sagt nichts darueber, was gespielt wird | **LUECKE** | Bewusste Verknappung oder Luecke — bitte entscheiden und dokumentieren. Ticket #110 (Spiel-Referenzen statt Freitext) zeigt in dieselbe Richtung: die Einladung koennte das tatsaechlich geplante Highlight-Spiel nennen statt eine Motto-Floskel. |
| QR-Fehlerfall bricht WYSIWYG: bei zu langer Party-URL zeigt die Karte auf dem Bildschirm ein weisses Feld mit Beschriftung 'PARTYSEITE', das exportierte PNG lae | **ANGEKLAGT** | Praktisch selten (party.machsleicht.de/<slug> bleibt weit unter 106 Byte), aber die Karte behauptet auf dem Schirm etwas anderes als sie exportiert — und der 'HIER ZUSAGEN'-Button samt Link-Zeile bleibt in beiden Faellen stehen. syncPartyArea sollte den Fehler |
| `/einladung/erstellen/` liest den Legacy-Key `machsleicht.birthdayProject.v1` als Prefill-Fallback — der einzige Schreiber dieses Keys ist in keiner einzigen HT | **TOT** | Toter Prefill-Zweig: der Fallback kann per Konstruktion nie feuern. Entweder js/birthday-project.js wieder einbinden (dann waere es ein VIERTER State-Speicher neben mlplan_v4_state, machsleicht-studio-v1 und dem Worker) oder den Block in erstellen/index.html l |
| `/einladung/erstellen/` ist ein vollstaendig paralleler Einladungs-Erzeuger: eigene Datenerfassung (Name/Datum/Uhrzeit/Ort/Telefon), eigener Foto-Crop, eigener  | **ANGEKLAGT** | Zweiter Funnel-Einstieg, der dieselben Party-Daten nochmal abfragt, die der Planer schon hat — Ticket #21 hat den Handoff aus dem Planer entfernt, die Seite selbst blieb aber breit verlinkt. Fuer #105 ('EIN Studio-Design') muss geklaert werden, ob /erstellen/  |
| Spiele-Katalog dreifach hardcodiert: `GAMES_E` in /einladung/erstellen/, `GAME_META` im party-worker, `GAME_META_*` in jedem Paket | **ANGEKLAGT** | Jede Spiel-Umbenennung ist ein 8-Dateien-Sweep mit garantiertem Drift. Ein generierter Katalog (wie _bundle.js fuer die Motto-Daten) waere die deterministische Loesung; mindestens gehoert eine Linter-Regel her, die die drei Kopien gegeneinander diffed. |
| Die 15 x /einladung/<motto>/vorlagen/-Seiten sind 100 % statisch hardcodiert (7 Texte mit {Platzhalter}) — kein Plan-Bezug, kein Studio-Bezug, und ausgerechnet  | **ANGEKLAGT** | Doppelte Wahrheit gegen data/motto: dort liegen pro Motto UND Altersgruppe massgeschneiderte Einladungstexte (invitationTemplate, 45 Stueck), hier sieben generische. Und die Platzhalter-Rhetorik ({Name}/{Alter}/{Datum}) ist Handarbeit fuer den Nutzer, obwohl d |
| `state.endTime` (Party-Ende) WIRD vom Studio konsumiert und gedruckt | **FREIGESPROCHEN** | Kein Handlungsbedarf. Der Kommentar dokumentiert sogar den Grund (leere Uhrzeit-Zeile im PNG) — Muster-Beispiel fuer ein erklaertes Feld. |
| `state.adresse` und `state.partyseite.url` werden auf dem lokalen Pfad korrekt konsumiert; die Party-URL wird beim Aktivieren real gesetzt | **FREIGESPROCHEN** | Kein Handlungsbedarf auf dem eigenen Geraet. Die Luecke sitzt ausschliesslich im URL-Handoff (eigener Eintrag oben). |
| Statische Kartentexte 'Du bist eingeladen!', 'HIER ZUSAGEN', 'PARTYSEITE' sowie die Emoji-Praefixe 📅/⏰/📍 | **FREIGESPROCHEN** | Erklaerte Konstanten im Sinne der Doktrin: sie behaupten keine Party-Fakten, sind ueberschreibbar und tragen keinen Datenanspruch. Kein Handlungsbedarf. |
| Foto-Handhabung beim Fremdgeraet-Handoff: kein Foto in der URL, und ohne echtes Planer-Foto wird das Demo-Kind nie gerendert oder geteilt | **FREIGESPROCHEN** | Kein Handlungsbedarf. Bewusste, kommentierte Entscheidung mit sauberem Ersatzweg (hero-motif statt Foto). Die zugehoerige Ticket-Historie (#29 photoRound sprengt iframe-URL) ist damit konsistent geschlossen. |


---

## Anhang B — Nachzuegler-Sweep 2: Spiel-Apps, planState, Zahlen-Versprechen

### spieleApps

| Gegenstand | Urteil | Konsequenz |
|---|---|---|
| URL-Param `name` (Worker → beide Spiel-Familien) | **LEBENDIG** | Einziger Param, bei dem Setzer und beide Leser wortgleich übereinstimmen. Quelle = party.childName (Wizard/Creator). Kein Handlungsbedarf. |
| URL-Param `ort` (Worker → 15 Legacy-Motto-Apps) | **TOT** | Der Worker sendet bewusst nichts, aber die Legacy-Apps behandeln „leer“ als „nicht gesetzt“ und springen in den Demo-Fallback. Das Adress-Gating wurde nur in core.js nachgezogen (core.js:204 `else if(real)pl.textContent='Wird nach deiner Zusage verraten 🤫';`), |
| URL-Param `tel` (Worker → 15 Legacy-Motto-Apps) | **TOT** | Schwerster Fund: der Worker setzt den Param nachweislich nie mit Wert, der Fallback schlägt IMMER zu. Der grüne Zusage-Button im Sieg-Screen öffnet damit bei jedem echten Gast einen WhatsApp-Chat mit einer fremden Nummer (piraten/whatsapp/index.html:1497 `wind |
| Hardcode `partyPlace` in allen 15 Legacy-Gast-Apps | **ANGEKLAGT** | Ein erfundener Ort wird echten Gästen als Treffpunkt angezeigt („📍 Stadtpark“, piraten:1475 `"📍 ", partyPlace`), während die Partyseite direkt darunter „🔒 Adresse erscheint nach deiner Zusage“ schreibt. Die Konstante erklärt sich nicht: keine Datenquelle, kein |
| Hardcode `rsvpPhone = "491701234567"` in allen 15 Legacy-Gast-Apps | **ANGEKLAGT** | Eine nicht erklärte, fremde Mobilnummer ist der einzige Zusage-Kanal des Spiels, sobald `tel` leer ankommt — also auf dem gesamten Partyseiten-Pfad. Weder Plan- noch Einladungs- noch Party-Daten liefern diese Zahl. Muss entweder aus party-Daten kommen oder der |
| Hardcode `childName = "Mia"` / `"Mattis"` in allen 15 Legacy-Gast-Apps | **ANGEKLAGT** | Bei leerem childName (API-Direktaufruf, Legacy-Party) rendert die Gast-App eine Einladung zu „Mias Feuerwehr-Party“. Fremdes Kind auf einer echten Einladung. core.js hat dafür den erklärten Fallback `'das Geburtstagskind'` (core.js:65) — die Legacy-Familie hat |
| URL-Param `date` — Formatvertrag zwischen den beiden Produzenten | **ANGEKLAGT** | Ein Param, zwei unvereinbare Formate. Über die Partyseite sieht der Gast „📅 2026-09-12“ statt eines Datums, und die WhatsApp-Zusage lautet „am 2026-09-12 um 14:00“ (piraten:1495). Nur core.js formatiert defensiv (core.js:199 `dd.textContent=isNaN(d)?ds:d.toLoc |
| URL-Param `time` (Worker → Legacy-Apps) | **ANGEKLAGT** | Auf der echten Einladung fällt das Wort „Uhr“ weg („🕑 14:00“), obwohl die Partyseite direkt darüber `${esc(party.time)} Uhr` schreibt. core.js hängt es an (core.js:202 `if(ts)tt.textContent=ts+' Uhr';`) — die Legacy-Familie nicht. Gleicher Wert, zwei Darstellu |
| `party.endTime` — vorhandene Daten, die die Spiel-URL nicht mitnimmt | **LUECKE** | Das Spiel kann nur eine Startzeit zeigen, wo die Legacy-Demo eine Spanne verspricht („14:00 – 17:00 Uhr“). Datenquelle existiert im selben Funktionsscope, wird aber nicht durchgereicht — der Param fehlt im Kontrakt, nicht die Information. |
| `?g=` als Gast-Name (core.js) — Produzent fehlt komplett | **LUECKE** | Die persönliche Ansprache im Magic-Moment feuert im Flaggschiff-Flow nie — obwohl der Worker den Vornamen zu diesem Zeitpunkt kennt: `guestPageFull(party, gamePhotoUrl, isPreview, invite)` hat `invite.n` im Scope (vgl. party-worker.js:547 `invites: party.invit |
| Namenskollision des Buchstabens `g` über drei Systeme | **ANGEKLAGT** | Derselbe Schlüssel bedeutet an drei Stellen drei Dinge, davon zwei exakt gegenläufig (Token, dessen Sinn die Namensvermeidung ist, vs. Klarname). Wer die LUECKE oben naiv schließt und `g` von der Partyseite an die Spiel-URL durchreicht, leakt entweder den Toke |
| `?k=` (Kind-Name) in core.js | **TOT** | Harmlos, weil `name` denselben Wert liefert und der Fallback dokumentiert ist — aber `k` selbst hat null Produzenten und ist damit toter Vertragsbestandteil. Beim Aufräumen streichen, damit der Kontrakt genau einen Namensschlüssel kennt. |
| `?nofoto` | **FREIGESPROCHEN** | Kein Produzent, aber als manueller Test-/Playtest-Schalter ausdrücklich erklärt. Erklärte Konstante im Sinne der Beweispflicht — kein Befund. |
| `?age` (Worker → Legacy-Familie) | **FREIGESPROCHEN** | Der Setzer deklariert die Asymmetrie ausdrücklich, also kein stiller Vertragsbruch. Anzumerken bleibt nur, dass die Deklaration inzwischen ungenau ist: 4 Legacy-Apps ignorieren ihn eben doch nicht mehr. Kommentar nachziehen. |
| `?age` auf dem /e/-Kurzlink-Pfad | **LUECKE** | Jede über /einladung/erstellen erzeugte Einladung läuft in den Default AGE=8 — die komplette Alters-Adaption (Simon-Länge, Halte-Gesten, YOUNG-Dramaturgie) ist auf diesem Produktpfad abgeschaltet. Zwei Produzenten desselben Kontrakts, einer kennt den Param nic |
| `?foto` — der Ersatz-Transportweg nach dem photoRound-Unfall | **LEBENDIG** | Der Fix ist konsistent: base64-in-URL wurde durch KV-Ablage plus kurze Bild-URL ersetzt, /api/invimg liest sowohl `invphoto:` (Partyseite) als auch `extimg:` (/e/-Produkt), und beide Spiel-Familien akzeptieren exakt diese URL-Form. Einziger Rest-Unterschied: d |
| base64-Zweig für `foto` in den 15 Legacy-Apps | **TOT** | Kein heute erzeugbarer Link kann diesen Pfad noch treffen; er lebt nur für Altbestand-URLs weiter, die zugleich nie auf ein core-Spiel zeigen können. Ungleichbehandlung als Nebeneffekt: derselbe Alt-Link mit `?f=<base64>` funktioniert in der Legacy-App und wir |
| Antwortfelder `foto` / `fotoDropped` von create-invite | **TOT** | Der Kommentar verspricht eine Warnung, die kein Client abholt; auch der Request-Parameter `foto` wird vom Client nie gesendet. Toter Vertragsanteil in Request UND Response — restlos entfernen, sonst simuliert der Code eine Absicherung, die es nicht gibt. |
| Spiel-Whitelist als drei handgepflegte Kopien derselben Tabelle | **ANGEKLAGT** | Vier Orte müssen von Hand synchron bleiben, damit ein Spiel überhaupt ausgeliefert wird — und sie sind es schon jetzt nur zufällig: der Worker führt `wappen-ritter`/`puzzle-dschungel` über IND_GAMES, serve-invite über GAME_WL, der Creator über GAMES_E. Genau d |
| Possessiv-Bildung im Spiel-Text vs. Worker-Helper | **ANGEKLAGT** | Auf einer Seite steht „Mats' Piraten-Einladung“ (Worker) und im Spiel „Mats s Piraten-Geburtstag“ bzw. „Matss“ — die dokumentierte Namensregel gilt nur auf der Hälfte des Renderings. Der String im Spiel leitet sich nicht aus dem Helper ab, sondern ist per Konk |
| Motto-Texte/Emojis in den Gast-Apps (z.B. „Feuerwehr-Party von …“) | **FREIGESPROCHEN** | Kein Befund: Die Datei IST das Motto, der Text kann per Konstruktion nicht zum Motto der Party divergieren. Bewusst festgehalten, damit dieser Hardcode nicht in künftigen Wellen erneut aufgegriffen wird. |

### planState

| Gegenstand | Urteil | Konsequenz |
|---|---|---|
| PRÄMISSE DES AUFTRAGS: „Der Planer konsumiert _src/kindergeburtstag-data.js" | **FREIGESPROCHEN** | Die Annahme ist falsch. Der Live-Planer speist sich aus (a) inline `const MOTTOS` in kindergeburtstag.html:1241, (b) `window.MOTTO_DATA` aus js/motto-data.js, (c) `fetch('/data/motto/<motto>-<gruppe>.json')`. `_src/kindergeburtstag-data.js` ist an keiner Stell |
| state.games — 5 Spielnamen aus dem toten Inline-Pool, aber sichtbar in der Fertig-Zusammenfassung | **ANGEKLAGT** | Zahlen-Widerspruch im Abschluss-Screen: bei Variante „minimal" zeigt der Plan 3 Spiele, die Zusammenfassung behauptet „5 ausgewählt" — und zwar 5 ANDERE Spiele (Inline-Namen wie „Tatzen-Memory" statt data/motto-Namen wie „🦁 Tierspuren-Pirsch"). Die Zahl ist fa |
| state.games — Schreibwege (Kachel-Toggle, Picker, Drawer-Button) nach dem Elite-Render unerreichbar | **TOT** | `togglePlanGame`, `openGameDetail` (samt „In den Plan aufnehmen"-Button) und die 150 Inline-Spiele sind nur im Millisekunden-Fenster zwischen synchronem Preview-Render und dem Ende des JSON-Fetch erreichbar. state.games ist damit praktisch eingefroren — was de |
| state.eliteOff — Spiel-Abwahl im Elite-Plan | **TOT** | Feld wird bei jedem Motto-/Alters-/Varianten-Wechsel zurückgesetzt (:1589, :1638, :1854) und in localStorage persistiert, hat aber null Wirkung. Toter Zustand, der bei jedem Review als „Spiel-Abwahl existiert" fehlgelesen werden kann. |
| state.customEntries — eigene Programmpunkte des V2-Zeitstrahls | **TOT** | Zwei parallele „eigener Programmpunkt"-Mechaniken; die persistierte `customEntries` ist die tote. _dev/EINLADUNGSMASCHINE-BRIEF.md:88 listet sie korrekt als tot — aber sie steht weiter im Default-State und wird mitgespeichert. |
| state.partyseite.slug — „party.machsleicht.de/<name><alter>" | **LUECKE** | Ein Wunsch-URL wird gepflegt, nirgends gezeigt und nie gesendet. Die reale Party-URL ist eine 12-stellige Zufalls-ID. Feld erklärt sich weder aus Datenquelle noch als Konstante — reiner Zombie aus dem Prototyp. |
| state.partyseite.rsvp / .wish — Feature-Schalter der Partyseite | **TOT** | Die Wunschlisten-Vorschau ist unerreichbarer Code, und `wish` steuert nichts. Für Wizard-Partys geht IMMER eine leere Wunschliste an /api/create — die Wunschliste entsteht erst nachträglich über den editUrl-Pfad. |
| state.partyseite.photos / .chat und state.invite.sent | **TOT** | Der Kommentar sagt „photos/chat raus", die Felder stehen trotzdem im Literal und werden bei jedem autoSave mitgeschrieben. Drei Felder ohne einen einzigen Leser im Produktionscode. |
| state.guests — Gästezahl überquert die Grenze zur Partyseite nicht | **LUECKE** | Die geplante Kinderzahl (Basis für Einkauf und Mengen) ist auf der Partyseite und im Komplettpaket unbekannt. Jede dort gedruckte Mengenangabe muss raten oder auf einen Default fallen. |
| state.location — Ort wirkt im Plan, kommt aber nie an der Partyseite an | **LUECKE** | Der Planer filtert Spiele und Tipps nach Ort; die Partyseite und das Paket wissen davon nichts. Ein reiner Draußen-Plan kann drüben mit Drinnen-Annahmen weitererzählt werden. |
| state.adresse — im Planer bewusst zugangsgeschützt, im Studio aufs geteilte Einladungsbild gedruckt | **ANGEKLAGT** | Zwei Einladungskanäle mit gegensätzlicher Datenschutz-Regel aus derselben Quelle. Das Worker-Gating (Adresse erst nach Zusage) wird durch das exportierte PNG umgangen, sobald das Bild in eine Gruppe geht. Für ein Produkt, das Gating als Feature verkauft, ist d |
| state.partyMessage — persönliche Nachricht wird vom Studio stillschweigend durch eine Motto-Floskel ersetzt | **ANGEKLAGT** | Wer im Planer „Kommt gern verkleidet" schreibt, findet auf der Einladungskarte einen generischen Motto-Satz. Die Partyseite zeigt die echte Nachricht, das Bild eine andere — dieselbe Party spricht mit zwei Stimmen. |
| state.invite.title / .body / .deadline / .whatsapp — Einladungstext des Planers | **LUECKE** | Der einzige Text, den die Mutter selbst formuliert hat, endet in WhatsApp/Mailto und sonst nirgends. Weder Partyseite noch Einladungsbild noch Paket kennen ihn — jede dieser Flächen erfindet ihren eigenen Einladungstext. |
| state.invite.photo — einziges Feld, das drei Konsumenten sauber bedient | **LEBENDIG** | Sauber. Nebenbefund: _dev/EINLADUNGSMASCHINE-BRIEF.md:88 listet `invite.photo` noch unter „TOT" — das Doc ist an dieser Stelle veraltet und würde einen Aufräum-Pass in die Irre führen. |
| Payload sendet `photo` nie — og:image der Partyseite bleibt für jede Wizard-Party leer | **LUECKE** | `hasPhoto` ist bei jeder aus dem Planer erzeugten Party false. Der geteilte Party-Link zeigt in WhatsApp keine Foto-Vorschau, obwohl ein gecropptes Foto vorliegt — genau im Moment, der viral wirken soll. |
| Undeklarierte Laufzeit-Felder: plan, gameChoice, ref, crewSynced, _planReadyFired, _maxStage, partyseite.url/editUrl/email | **LEBENDIG** | Das Default-Literal ist kein vollständiges Schema. Wer `mlplan_v4_state` aus Zeile 1383 ff. liest, übersieht acht produktive Felder — darunter die viralen (`ref`), die Spiel-Wahl (`gameChoice`) und den kompletten Plan (`plan`). Jede Migration/Validierung auf B |
| Vier parallele Spiel-Datenbestände für dieselben 15 Mottos | **ANGEKLAGT** | Führend ist (3): `renderElitePlan` ersetzt `#planBody` komplett. (1) ist tote Handpflege mit Seiteneffekt (speist state.games → falsche „5 ausgewählt"), (2) erreicht nur noch den Custom-Motto-Fallback, (4) ist reine Altlast. Die Dublette ist der Defekt: vier O |
| Null Überschneidung der Spielnamen zwischen _src/kindergeburtstag-data.js und data/motto (Beispiel safari) | **ANGEKLAGT** | Drei disjunkte Spielkataloge für ein Motto. Es gibt keinen Merge-Pfad, keinen ID-Schlüssel, keinen Test, der sie in Beziehung setzt — Redaktion an einem Ort kann per Konstruktion nicht an den anderen ankommen. |
| js/motto-data.js deckt nur piraten ab — die 14 anderen Mottos zeigen im Detail-Drawer eine Ausrede | **LUECKE** | Der Header von js/motto-data.js nennt sich „Single Source of Truth für die Wizard-Spieldaten", ist aber zu 1/15 gefüllt. Der Ausreden-Text wird nur deshalb kaum gesehen, weil der Elite-Render das Drawer-Markup vorher wegräumt — die Lücke ist also zusätzlich un |
| Custom-Motto rendert Piraten-Spiele unter fremdem Namen | **ANGEKLAGT** | Eine „Pferde-Hof"-Party bekommt „Walk the Plank" und „Kanonenkugeln". Es ist der einzige Pfad, auf dem die Inline-Spiele überhaupt noch gerendert werden — und ausgerechnet dort sind sie thematisch falsch. |
| WIZ_GAMES (Wizard) vs. IND_GAMES/GAME_CATALOG/GAME_META (Worker) — vollständige Handpflege-Dublette | **ANGEKLAGT** | Heute deckungsgleich — die Dublette ist trotzdem der Defekt: Name, Untertitel und Emoji jedes Spiels stehen zweimal im Repo, in zwei getrennt deploybaren Artefakten (Netlify vs. Cloudflare Worker). Der erste Deploy, der nur eine Seite mitnimmt, erzeugt Kacheln |
| SZ_THEMES (9 Themen) vs. data/schatzsuche.json (15 Themen) | **ANGEKLAGT** | Der Live-Bestand ist 15, die Altlast sagt 9. Solange validate-all.sh die 9 als Wahrheit zählt (s. nächster Eintrag), misst das Gate den falschen Bestand. |
| validate-all.sh gatet auf zwei tote Dateien | **ANGEKLAGT** | Das deterministische Gate (Stufe 1 nach Helfer V4.1) prüft Zahlen und ID-Konsistenz gegen Dateien, die kein Browser lädt. Der Kommentar in Zeile 96 beweist, dass die Umstellung begonnen und in derselben Stufe abgebrochen wurde — halb migriertes Gate ist gefähr |
| js/birthday-project.js — komplettes Modul, von keiner Seite geladen | **TOT** | Alle Felder des dort dokumentierten Schemas sind tot: `version, createdAt, updatedAt, source`, `theme{slug,label,emoji,color}`, `child{firstName,age}`, `party{guestCount,date,startTime,endTime,locationType,address}`, `preferences{durationMinutes,budgetLevel,ma |
| modules.treasure (und alle modules.*) aus BirthdayProject | **TOT** | Die im Auftrag vermutete „Schatz-Wahl, die nie an die Partyseite geht" ist noch eine Stufe schlimmer: Es gibt gar keine Schatz-Wahl. `modules.treasure` wird nie gesetzt, nie gelesen, nie gesendet. Der Kommentar im Paket-Kern verweist auf ein Feld, das seit dem |
| einladung/erstellen liest `machsleicht.birthdayProject.v1` — Leser ohne Schreiber | **LUECKE** | Ein Prefill-Pfad, der garantiert nie feuert. Wer den Funnel debuggt, sieht Code für eine Cross-Tool-Übergabe, die es nicht gibt — und übersieht, dass die reale Übergabe ausschließlich über `mlplan_v4_state` läuft. |
| BirthdayProject.toPartyPayload() — Vertrag ist gegenüber /api/create um Jahre veraltet | **TOT** | Die Funktion würde, falls jemand sie reaktiviert, eine Party ohne Altersgruppe, ohne Ambition, ohne Motto-ID, ohne Spiel, ohne Gästeliste und ohne Foto anlegen — und die persönliche Nachricht durch die Floskel aus Zeile 164 ersetzen. Genau die Defekte, die 202 |
| _dev/docs/WORKER-CONTRACT.md schickt Integratoren auf die tote Funktion | **ANGEKLAGT** | Die technische Referenz benennt als kanonischen Weg genau das Modul, das nirgends geladen wird und dessen Payload den Vertrag verletzt. Wer sich daran hält, baut den nächsten Integrationsbruch nach Anleitung. |
| js/theme-registry.js — 310 Zeilen ohne einen einzigen Konsumenten | **TOT** | Ein zweites totes Registry neben BirthdayProject, das dieselben Modul-Namen führt. Beide zusammen erzeugen den Eindruck einer Modul-Architektur, die im Live-System nicht existiert — der reale Zusammenhalt ist ein einzelner localStorage-Key plus ein POST. |
| state.crew → invites: der einzige sauber durchgezogene Grenzübertritt | **LEBENDIG** | Musterfall: Wert, Kappungs-Konstante, Empfänger und Rücksynchronisation sind alle belegt und aneinander gebunden. Die Konstante 30 ist erklärt („= MAX_GUESTS im Worker"). Als Referenz brauchbar, an der die anderen Felder gemessen werden sollten. |
| state.stage / .step / .lastSaved / .eliteVariant / .age / .exactAge / .name / .date / .time / .endTime / .motto / .crewText | **LEBENDIG** | Kein Handlungsbedarf. Diese zwölf Felder haben Schreiber, Leser und — wo sie die Grenze überqueren — einen benannten Empfänger im Worker. |
| Studio liest 10 Felder, ignoriert 18 — die Grenze ist nirgends dokumentiert | **LUECKE** | Das Studio zieht eine Untermenge, ohne dass irgendwo steht, welche — die einzige Absicherung ist ein Kommentar-Kreuzverweis (:1756 „A3-8: Gate-Paritaet mit dem Funnel-CTA-Toggle …"). Jedes neue Planer-Feld ist per Default unsichtbar im Studio, und niemand merk |

### zahlen

| Gegenstand | Urteil | Konsequenz |
|---|---|---|
| "15 Mottos" — Kindergeburtstag/Einladung, auf allen Verkaufsflächen | **FREIGESPROCHEN** | Bereits maschinell gedeckt: Stufe 31 (_dev/scripts/check-sichtbarer-text.py:121 ZAHL-Regex, SOLL_MOTTOS=len(glob('data/motto/*-mittel.json'))). ABER nur im Glob '**/*.html' — die identische Zahl in js/index.js ist ungeprüft (s. LUECKE-Eintrag). |
| "15 Themen" — Schatzsuche | **FREIGESPROCHEN** | Bereits gedeckt durch Stufe 31 (SOLL_THEMEN = len(data/schatzsuche.json)) — für HTML. js/index.js:1 bleibt außerhalb des Scopes. |
| "225 Stationen" — Schatzsuche-Feature-Chip | **FREIGESPROCHEN** | Arithmetisch exakt, aber Aggregat über ALLE Themen UND alle Altersgruppen — ein Nutzer bekommt pro Lauf 5. Stufe 35 kann es trivial rechnen: SOLL = sum(len(t['stations'][a]) for t in schatzsuche.json for a in ('klein','mittel','gross')). |
| "3 Altersgruppen" — Kindergeburtstag | **FREIGESPROCHEN** | Stufe 35 rechenbar aus dem Suffix-Set von data/motto/*.json (len(set(suffix))). Heute ungeprüft. |
| "150+ Spiele" / "150+ Spielideen" — Kindergeburtstag-Planer | **FREIGESPROCHEN** | Auch bei strengster Lesart (344 unique) deutlich über 150. Stufe 35 kann offene Untergrenzen prüfen: FAIL nur wenn Ist < N bei Muster '\d+\+ Spiele'. |
| "5 Themen" — Einschulungs-Planer (Schema.org auf index.html) | **FREIGESPROCHEN** | Schwächster Beleg im Freispruch-Set: die Datenquelle ist ein JS-Literal, kein data/-File. Stufe 35 müsste js/einschulung.js parsen oder die Themen nach data/einschulung.json ziehen — sonst bleibt die Zahl dauerhaft unbeweisbar. |
| "7 Vorlagen" pro Motto — /einladung-Top-Hub | **FREIGESPROCHEN** | Ideal für Stufe 35: count('class="copybtn"') je Vorlagen-Seite muss die Zahl im eigenen <title>/og:title UND die Zahl im Hub-Satz treffen — drei Stellen, eine Wahrheit. |
| "Alle 15 Motto-Einladungen" — index.html Deep-Link | **FREIGESPROCHEN** | Stufe 35 kann Hub-Karten zählen (Links minus Whitelist) und gegen die getippte Zahl auf index.html prüfen. |
| "je 2 Spiele zur Auswahl" — Spiel-Einladung, index.html | **ANGEKLAGT** | Untertreibung um Faktor 2,5 — kostet Conversion statt sie zu erschleichen, aber es ist derselbe Defekt-Typ. Stufe 35 exakt rechenbar: len(WIZ_GAMES[motto]) gegen jede „je N Spiele"-Formulierung. |
| "15 Mottos × 2 Spiele" — Produktkarten-Chip in js/index.js | **ANGEKLAGT** | Doppelt kritisch: js/index.js liegt AUSSERHALB des Stufe-31-Globs ('**/*.html'), diese Zahl kann heute weder ein Linter noch ein HTML-Grep finden. Stufe 35 muss js/*.js in den Scope nehmen. |
| "8 Familien-Checklisten" — FAQ-Schema auf index.html | **ANGEKLAGT** | Die Zahl hat GAR KEINE Datenquelle — sie ist weder aus data/ noch aus dem Dateibestand noch aus den eigenen Links ableitbar. Stufe 35 kann sie erst prüfen, wenn eine explizite Liste existiert (data/checklisten.json oder ein markierter Link-Block auf index.html |
| "4–7 Spiele mit Anleitung" — kindergeburtstag.html Produktversprechen | **ANGEKLAGT** | Beide Enden verletzt: minimal-Varianten liefern nur 3 (Untererfüllung), standard bis zu 10. Stufe 35 rechenbar: min/max über variants[].games gegen jede getippte Spanne „N–M Spiele". |
| "3, 5 oder 7 Stationen" + tote ?stationen-Links — schatzsuche.html (Nachbarfläche der 225-Behauptung) | **ANGEKLAGT** | Alle drei „Varianten"-Kacheln landen auf identischer Ausgabe — der Nutzer klickt „Groß · 7 Stationen" und bekommt 5. Das ist die härteste Fundstelle des Laufs und relativiert die (rechnerisch korrekte) „225 Stationen"-Behauptung auf index.html. Stufe 35 brauch |
| LUECKE: js/*.js liegt komplett außerhalb der maschinellen Zahlenprüfung | **LUECKE** | Die Startseite ist ein React-Mount (index.html:207 `<div id="root">`) — was der Nutzer wirklich sieht, steht in js/index.js, und genau diese Datei ist zahlenblind. Der ANGEKLAGTE „15 Mottos × 2 Spiele"-Chip beweist, dass die Lücke bereits einen Live-Defekt trä |
| LUECKE: ZAHL-Regex der Stufe 31 deckt nur Themen/Mottos und nur zweistellige Zahlen | **LUECKE** | JA — Stufe 35 ist machbar und lohnt sich. Konkret: Regex auf `\b(\d{1,4})\s*\+?\s+(Mottos?\|Motto-Welten\|Motto-Einladungen\|Themen\|Spiele\|Spielideen\|Stationen\|Vorlagen\|Altersgruppen)\b` plus eine SOLL-Tabelle mit je einer Datenquelle: Mottos=len(data/mot |
