diff --git a/kindergeburtstag.html b/kindergeburtstag.html
index 2cc946f5..a0a5db54 100644
--- a/kindergeburtstag.html
+++ b/kindergeburtstag.html
@@ -837,6 +837,48 @@ window.plausible.init=function(){};window.plausible.q=[];
   </div>
 </section>
 
+<!-- STAGE 3: PLAN -->
+<section class="stage" id="stage3">
+  <div class="sticky-cta" id="stickyCta">
+    <div class="sticky-cta__label" id="stickyLabel">✏️ Eckdaten ändern<small>Name, Datum, Gäste, Alter — der Plan passt sich live an</small></div>
+    <button id="stickyBtn" onclick="goStage(2)">Ändern</button>
+  </div>
+
+  <section class="plan-hero">
+    <span class="plan-hero__badge"><span id="planBadgeEmoji">🏴‍☠️</span> <span id="planBadgeName">Piraten</span> · <span id="planBadgeAge">6–8 Jahre</span></span>
+    <h2>So sieht ein <em>fertiger Plan</em> aus</h2>
+    <p>Schon befüllt mit sinnvollen Standard-Werten. Du kannst jeden Programmpunkt verändern, eigene Punkte einfügen und alle Details anpassen.</p>
+  </section>
+
+  <div id="gameTeaser" style="margin:14px 0 0;padding:14px 16px;background:#FFF8EC;border:2px dashed #D4812A;border-radius:14px;display:flex;align-items:center;gap:12px;flex-wrap:wrap">
+    <span style="font-size:26px">🎮</span>
+    <div style="flex:1;min-width:200px"><b>Deine Spiel-Einladung ist schon bereit</b><br><small style="color:#7a6a50">Gäste spielen sie im Handy — am Ende springt das Foto deines Kindes heraus.</small></div>
+    <button type="button" onclick="try{tryPsGame((state.gameChoice&&state.gameChoice.indexOf('-')>0)?state.gameChoice:(psMid()+'-klassik'))}catch(e){}" style="border:none;background:#FF6F00;color:#fff;border-radius:999px;padding:10px 18px;font-family:'Nunito';font-weight:800;font-size:14px;cursor:pointer">▶ Jetzt anspielen</button>
+  </div>
+
+  <div class="plan">
+    <header class="plan__header">
+      <div class="plan__header-text">
+        <h2 class="plan__title"><span class="plan__name" id="planName1">dein Kind</span> <span id="planMottoName">Piraten</span>-Geburtstag</h2>
+        <div class="plan__meta">
+          <span id="planDate1">Termin noch offen</span>
+          <span><span id="planGuests1">6</span> Gäste</span>
+          <span id="planLocation1">zuhause</span>
+        </div>
+      </div>
+      <button class="plan__edit-btn" onclick="goStage(2)">✏️ Eckdaten ändern</button>
+    </header>
+    <div class="plan__body" id="planBody"></div>
+  </div>
+
+  <div class="stage-advance">
+    <p class="stage-advance__hint" id="planAdvanceHint">Dein Plan steht. Willst du die Kinder gleich einladen?</p>
+    <button class="stage-advance__btn" id="planAdvanceBtn" onclick="goStage(4)">Kinder einladen →</button>
+    <button class="stage-advance__skip" id="planAdvanceSkip" onclick="goStage(5)">Ohne Einladung fertig machen →</button>
+    <div style="margin-top:18px"><button class="notfall-cta" onclick="try{if(window.plausible)plausible('notfall_cta_click')}catch(e){}; location.href='/kindergeburtstag-last-minute'">🚨 Geburtstag ist schon morgen? → Last-Minute-Plan in 48 Stunden</button></div>
+  </div>
+</section>
+
 <!-- STAGE 4: EINLADUNG & PARTYSEITE (gemergt 15.06.2026 — ein Datensatz, ein teilbarer Link) -->
 <section class="stage" id="stage4">
   <div class="back-anchor"><button onclick="goStage(3)">← Zurück zum Plan</button></div>
@@ -996,53 +1038,11 @@ window.plausible.init=function(){};window.plausible.q=[];
   </div>
 
   <div class="stage-advance">
-    <p class="stage-advance__hint">Fertig mit der Einladung? Dann mach dich fertig für den Tag — Plan, Einkauf und Materialien warten.</p>
+    <p class="stage-advance__hint">Fertig mit der Einladung? Dann schau, was du dir für den Tag mitnimmst — online oder gedruckt.</p>
     <button class="stage-advance__btn" onclick="goStage(5)">Weiter: Fertig machen →</button>
   </div>
 </section>
 
-<!-- STAGE 3: PLAN -->
-<section class="stage" id="stage3">
-  <div class="sticky-cta" id="stickyCta">
-    <div class="sticky-cta__label" id="stickyLabel">✏️ Eckdaten ändern<small>Name, Datum, Gäste, Alter — der Plan passt sich live an</small></div>
-    <button id="stickyBtn" onclick="goStage(2)">Ändern</button>
-  </div>
-
-  <section class="plan-hero">
-    <span class="plan-hero__badge"><span id="planBadgeEmoji">🏴‍☠️</span> <span id="planBadgeName">Piraten</span> · <span id="planBadgeAge">6–8 Jahre</span></span>
-    <h2>So sieht ein <em>fertiger Plan</em> aus</h2>
-    <p>Schon befüllt mit sinnvollen Standard-Werten. Du kannst jeden Programmpunkt verändern, eigene Punkte einfügen und alle Details anpassen.</p>
-  </section>
-
-  <div id="gameTeaser" style="margin:14px 0 0;padding:14px 16px;background:#FFF8EC;border:2px dashed #D4812A;border-radius:14px;display:flex;align-items:center;gap:12px;flex-wrap:wrap">
-    <span style="font-size:26px">🎮</span>
-    <div style="flex:1;min-width:200px"><b>Deine Spiel-Einladung ist schon bereit</b><br><small style="color:#7a6a50">Gäste spielen sie im Handy — am Ende springt das Foto deines Kindes heraus.</small></div>
-    <button type="button" onclick="try{tryPsGame((state.gameChoice&&state.gameChoice.indexOf('-')>0)?state.gameChoice:(psMid()+'-klassik'))}catch(e){}" style="border:none;background:#FF6F00;color:#fff;border-radius:999px;padding:10px 18px;font-family:'Nunito';font-weight:800;font-size:14px;cursor:pointer">▶ Jetzt anspielen</button>
-  </div>
-
-  <div class="plan">
-    <header class="plan__header">
-      <div class="plan__header-text">
-        <h2 class="plan__title"><span class="plan__name" id="planName1">dein Kind</span> <span id="planMottoName">Piraten</span>-Geburtstag</h2>
-        <div class="plan__meta">
-          <span id="planDate1">Termin noch offen</span>
-          <span><span id="planGuests1">6</span> Gäste</span>
-          <span id="planLocation1">zuhause</span>
-        </div>
-      </div>
-      <button class="plan__edit-btn" onclick="goStage(2)">✏️ Eckdaten ändern</button>
-    </header>
-    <div class="plan__body" id="planBody"></div>
-  </div>
-
-  <div class="stage-advance">
-    <p class="stage-advance__hint">Dein Plan steht. Willst du die Kinder gleich einladen?</p>
-    <button class="stage-advance__btn" onclick="goStage(4)">Kinder einladen →</button>
-    <button class="stage-advance__btn" onclick="goStage(5)" style="margin-top:10px;background:transparent;color:var(--ink);border:2px solid var(--line)">Ohne Einladung fertig machen →</button>
-    <div style="margin-top:18px"><button class="notfall-cta" onclick="try{if(window.plausible)plausible('notfall_cta_click')}catch(e){}; location.href='/kindergeburtstag-last-minute'">🚨 Geburtstag ist schon morgen? → Last-Minute-Plan in 48 Stunden</button></div>
-  </div>
-</section>
-
 <!-- STAGE 5: FERTIG (war stage6 — nach Merge von Einladung+Partyseite) -->
 <section class="stage" id="stage5">
   <div class="done">
@@ -1484,7 +1484,8 @@ window.plausible.init=function(){};window.plausible.q=[];
     partyseite: { slug:'lukas7', rsvp:true, photos:false, wish:false, chat:false, active:false },   // photos/chat raus (F4: kein Worker-Backend)
     step: 1,
     eliteVariant: 'standard',
-    lastSaved: null
+    lastSaved: null,
+    _so: 8   // Schritt-Ordnung, unter der dieser Stand entstand (08.09.2026, Block 8: Plan vor Einladung). Siehe loadSavedState.
   };
 
   function defaultDate(){
@@ -1597,6 +1598,11 @@ window.plausible.init=function(){};window.plausible.q=[];
       if(!saved) return null;
       const parsed = JSON.parse(saved);
       if(!parsed.motto || typeof parsed.motto !== 'object' || !parsed.motto.id) return null;   // Typ-Guard: altes String-Motto-Schema wuerde sonst pickAge/renderPlanPreview mit TypeError brechen (Befund Kritik-Welle)
+      // 08.09.2026 (Pruefstand-Befund zu Block 8): _maxStage ist die weiteste erreichte Stufe und wird per ORD()
+      // verglichen, also nach Position. Vor Block 8 lag die Einladung an Position 2 — ein Stand von gestern traegt
+      // deshalb 4, waehrend der Plan heute Position 3 hat. Ohne diese Zeile laese goStage(3) den ersten Plan eines
+      // Rueckkehrers als Rueckschritt und zaehlte ihn nicht. Einmal auf Anfang setzen; _so haelt fest, dass es schon geschah.
+      if(parsed._so !== 8){ parsed._maxStage = 1; parsed._so = 8; }
       return parsed;
     } catch(e){ return null }
   }
@@ -2334,30 +2340,43 @@ window.plausible.init=function(){};window.plausible.q=[];
   // ===== STAGE NAV =====
   const STAGE_ORDER = [1, 2, 3, 4, 5];               // logische Schritt-Reihenfolge der Stage-IDs (08.09.2026: Plan VOR Einladung, s. revealPlan)
   const ORD = id => STAGE_ORDER.indexOf(id);
+  // "Die Einladung ist erledigt" heisst: die Partyseite ist aktiv, Gaeste koennen zusagen. Dieselbe Wahrheit
+  // benutzen die Fertig-Seite (doneBadge) und die mitlaufende Leiste — deshalb hier einmal benannt.
+  const EINLADUNG_LAEUFT = () => !!(state.partyseite && state.partyseite.active);
   function goStage(n){
     if(n > 5) n = 5;   // Migration: alte localStorage-States (6-Stage-Funnel) auf den gemergten 5-Stage-Funnel klemmen
     state.stage = n;
     document.querySelectorAll('.stage').forEach(s => s.classList.remove('active'));
     const el = document.getElementById('stage'+n);
     if(el) el.classList.add('active');
-    // Phase B (Bolle 13.07.): logische Ordnung Motto -> Eckdaten -> EINLADUNG(4) -> PLAN(3) -> Fertig.
+    // Block 8 (Bolle 08.09.): logische Ordnung Motto -> Eckdaten -> PLAN(3) -> EINLADUNG(4) -> Fertig.
     // Stage-IDs bleiben stabil (Resume/Analytics), nur Reveal + Dots folgen STAGE_ORDER.
     const cur = ORD(n);
-    STAGE_ORDER.forEach(id => { if(id >= 3 && ORD(id) <= cur){ const rs = document.getElementById('stage'+id); if(rs && !rs.classList.contains('revealed')){ rs.classList.add('revealed'); if(id !== n && id === 4){ try{renderInvitePreview()}catch(e){} try{renderPartyseitePreview()}catch(e){} } } } });
+    // Ein Abschnitt wird aufgedeckt, wenn der Nutzer ihn betritt oder daran vorbei ist — mit einer Ausnahme:
+    // Stufe 4. Wer vom Plan aus "ohne Einladung" abkuerzt, soll den Editor nicht zwischen Plan und Abschluss
+    // stehen haben (Re-Check 9, MINOR 1); wer die Einladung dagegen schon laufen hat, soll sie sehen und
+    // wieder anspringen koennen, auch aus einem Stand der alten Reihenfolge (MINOR 3). Stufe 4 haengt
+    // deshalb nicht an der Position, sondern an der Leistung: sie liegt HINTER dem Plan und waere ueber
+    // einen Positionsvergleich fuer einen Rueckkehrer nie zu erreichen.
+    const _aufdecken = id => id === n || (id === 4 ? (EINLADUNG_LAEUFT() && cur >= ORD(3)) : ORD(id) < cur);
+    STAGE_ORDER.forEach(id => { if(id >= 3 && _aufdecken(id)){ const rs = document.getElementById('stage'+id); if(rs && !rs.classList.contains('revealed')){ rs.classList.add('revealed'); if(id !== n && id === 4){ try{renderInvitePreview()}catch(e){} try{renderPartyseitePreview()}catch(e){} } } } });
     for(let i=1; i<=5; i++){
       const dot = document.getElementById('navDot'+i);
       if(!dot) continue;
+      const s = document.getElementById('stage'+i);
+      const _zu = i >= 3 && !(s && s.classList.contains('revealed'));   // nie geoeffnet
       dot.classList.remove('done','active','locked');
       let _locked = false;
-      if(ORD(i) < cur) dot.classList.add('done');
-      else if(i === n) dot.classList.add('active');
-      else { const s = document.getElementById('stage'+i); if(i >= 3 && !(s && s.classList.contains('revealed'))){ dot.classList.add('locked'); _locked = true; } }
+      if(i === n) dot.classList.add('active');
+      else if(_zu && ORD(i) > cur){ dot.classList.add('locked'); _locked = true; }   // liegt noch vor dem Nutzer
+      else if(!_zu && ORD(i) < cur) dot.classList.add('done');
+      // Uebersprungen (zu, aber schon dahinter): weder erledigt noch gesperrt — anklickbar zum Nachholen.
       dot.setAttribute('tabindex', _locked ? '-1' : '0');   // A11y: gelockte Dots aus der Tab-Reihenfolge nehmen
     }
     const ds = document.getElementById('demoStage'); if(ds) ds.textContent = (cur + 1);
     // plan_ready = ECHTER personalisierter Aha (Name gesetzt). Deep-Link/Nav-Sprung/Resume/Zurueck ohne Name zaehlen NICHT (sonst luegt die North-Star-Zahl).
     if(n === 3 && !state._planReadyFired && state.name && state.name.trim()){ state._planReadyFired = true; try{ if(window.plausible){ const _mid=(state.motto&&state.motto.id||'').indexOf('custom')===0?'custom':(state.motto&&state.motto.id||''); plausible('plan_ready', {props:{motto:_mid, personalized:true}}); } }catch(e){} }
-    if(n === 3){ try{ renderPlanPreview(); }catch(e){}   // Final-Gate M1: Eckdaten-Aenderungen (v.a. Gaeste) beim Rueckweg in den Plan uebernehmen
+    if(n === 3){ try{ renderPlanPreview(); }catch(e){} try{ syncPlanAdvance(); }catch(e){}   // Final-Gate M1: Eckdaten-Aenderungen (v.a. Gaeste) beim Rueckweg in den Plan uebernehmen
       if(window.__syncStickyMorph){ try{ window.__syncStickyMorph(); }catch(e){} } }
     if(n === 4){ renderInvitePreview(); renderPartyseitePreview(); }
     if(n === 5) renderDoneSummary();
@@ -2367,11 +2386,25 @@ window.plausible.init=function(){};window.plausible.q=[];
     if(el) el.scrollIntoView({behavior:'smooth', block:'start'});  // Scroll-Funnel: zum Abschnitt scrollen statt toggeln
   }
   function jumpStage(n){
-    // Nur zu bereits freigeschalteten Abschnitten springen — kein Voraus-Springen im Funnel (Stage 1/2 immer offen).
-    if(n >= 3){ const t = document.getElementById('stage'+n); if(!(t && t.classList.contains('revealed'))) return; }
+    // Kein Voraus-Springen im Funnel (Stage 1/2 immer offen). Zurueck auf einen uebersprungenen Schritt
+    // dagegen schon — sonst waere die abgewaehlte Einladung ueber die Punkte nicht mehr nachzuholen.
+    if(n >= 3){ const t = document.getElementById('stage'+n); if(!(t && t.classList.contains('revealed')) && ORD(n) > ORD(state.stage)) return; }
     return goStage(n);
   }
 
+  // Der Abschluss unter dem Plan spricht denselben Zustand wie die mitlaufende Leiste und die Fertig-Seite.
+  // Vorher war er statisches Markup: Ein Rueckkehrer mit laufender Partyseite wurde erneut zum Einladen
+  // geschickt, waehrend die Leiste daneben schon "Fertig machen" anbot (Re-Check 9, MINOR 5).
+  function syncPlanAdvance(){
+    const a = EINLADUNG_LAEUFT();
+    const h = document.getElementById('planAdvanceHint');
+    if(h) h.textContent = a ? 'Deine Einladung läuft schon — dann fehlt nur noch der letzte Schliff.' : 'Dein Plan steht. Willst du die Kinder gleich einladen?';
+    const b = document.getElementById('planAdvanceBtn');
+    if(b){ b.textContent = a ? 'Fertig machen →' : 'Kinder einladen →'; b.onclick = function(){ goStage(EINLADUNG_LAEUFT() ? 5 : 4); }; }
+    const s = document.getElementById('planAdvanceSkip');
+    if(s) s.style.display = a ? 'none' : '';
+  }
+
   // ===== WIZARD =====
   function openWizard(jumpTo){
     document.getElementById('drawer').classList.add('open');
