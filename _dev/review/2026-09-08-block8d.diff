diff --git a/kindergeburtstag.html b/kindergeburtstag.html
index a0a5db54..423c4147 100644
--- a/kindergeburtstag.html
+++ b/kindergeburtstag.html
@@ -376,7 +376,12 @@ window.plausible.init=function(){};window.plausible.q=[];
   .stage-advance__hint{color:#888;font-size:13px;margin-bottom:14px}
   .stage-advance__btn{background:#FF6F00;color:#fff;border:none;padding:16px 32px;border-radius:999px;font-family:'Nunito';font-weight:800;font-size:16px;cursor:pointer;box-shadow:0 6px 20px rgba(0,0,0,.15);transition:transform .15s;display:inline-flex;align-items:center;gap:10px}
   .stage-advance__btn:hover{transform:scale(1.03)}
-  .stage-advance__skip{display:block;margin-top:14px;background:none;border:none;color:#888;font-family:'Nunito';font-weight:600;font-size:13px;cursor:pointer;text-decoration:underline}
+  /* Diese Klasse stand seit je da und wurde nie dargestellt — Block 8c ist ihr erster Auftritt (Abwahl-Weg
+     unter dem Plan). Bei 375 px gemessen und dreifach berichtigt: #888 auf dem Seitengrund ergab 3.37:1
+     (AA verlangt 4.5 fuer 13 px), die Klickflaeche war 18 px hoch (WCAG 2.5.8: 24), und ein block-Knopf mit
+     automatischer Breite folgt keinem text-align — er stand links statt unter dem Hauptknopf. Jetzt 4.98:1,
+     216x36, Mitte deckungsgleich. #7a6a50 ist die Palettenfarbe aus dem Spiel-Hinweis dieser Datei. */
+  .stage-advance__skip{display:block;margin:14px auto 0;padding:10px 14px;background:none;border:none;color:#7a6a50;font-family:'Nunito';font-weight:600;font-size:13px;cursor:pointer;text-decoration:underline}
 
   /* Back-Anchor on Stage 4+5 */
   .back-anchor{max-width:1100px;margin:0 auto;padding:20px 20px 0}
@@ -1106,8 +1111,8 @@ window.plausible.init=function(){};window.plausible.q=[];
     <h3>So funktioniert's</h3>
     <ol class="seo-base__steps">
       <li><strong>Motto &amp; Alter wählen.</strong> Wir filtern Spiele, Deko und Ablauf passend für 3–5, 6–8 oder 9–12 Jahre — keine Auswahl, die für die Altersgruppe gar nicht funktioniert.</li>
-      <li><strong>Einladung &amp; Partyseite verschicken.</strong> Spiel-Einladung wählen, Foto rein, aktivieren — der Link geht per WhatsApp an die Eltern, Zusagen kommen automatisch zurück.</li>
       <li><strong>Plan feinschleifen &amp; drucken.</strong> Tagesplan mit Uhrzeiten, 4–7 Spiele mit Anleitung, Einkaufsliste mit Preisen und Snack-Mengen — berechnet für genau deine Gästezahl. Kostenlos, ohne Anmeldung.</li>
+      <li><strong>Einladung &amp; Partyseite verschicken.</strong> Spiel-Einladung wählen, Foto rein, aktivieren — der Link geht per WhatsApp an die Eltern, Zusagen kommen automatisch zurück.</li>
     </ol>
 
     <h3>Welches Programm passt zu welchem Alter?</h3>
@@ -1602,7 +1607,16 @@ window.plausible.init=function(){};window.plausible.q=[];
       // verglichen, also nach Position. Vor Block 8 lag die Einladung an Position 2 — ein Stand von gestern traegt
       // deshalb 4, waehrend der Plan heute Position 3 hat. Ohne diese Zeile laese goStage(3) den ersten Plan eines
       // Rueckkehrers als Rueckschritt und zaehlte ihn nicht. Einmal auf Anfang setzen; _so haelt fest, dass es schon geschah.
-      if(parsed._so !== 8){ parsed._maxStage = 1; parsed._so = 8; }
+      // Die alte Ordnung war 1,2,4,3,5. Wer bis m kam, hat alle Stufen mit ORD_alt <= ORD_alt(m) gesehen;
+      // der neue Merker ist der laengste Anfang der NEUEN Ordnung, der ganz in dieser Menge liegt.
+      // 1->1, 2->2, 4->2 (Einladung gesehen, Plan nicht), 3->4 (Plan gesehen, also auch die Einladung
+      // davor), 5->5. Ein blosses Zuruecksetzen auf 1 liess Rueckkehrer ihre Stufen ein zweites Mal
+      // zaehlen und hob den Trichter genau im Umstellungsfenster (Re-Check 10, MINOR 1).
+      // Bei 4 bleibt genau ein Ereignis zu viel. Das ist keine Ungenauigkeit, sondern eine Eigenschaft
+      // der Aufgabe: die alte Besuchsmenge {1,2,4} ist kein Anfang der neuen Ordnung, eine verlustfreie
+      // Abbildung gibt es also nicht. Gewaehlt ist die Richtung, die lieber einmal zu viel zaehlt als
+      // einmal zu wenig — ein fehlender Schritt waere im Trichter unsichtbar, ein zusaetzlicher nicht.
+      if(parsed._so !== 8){ parsed._maxStage = ({1:1, 2:2, 4:2, 3:4, 5:5})[parsed._maxStage] || 1; parsed._so = 8; }
       return parsed;
     } catch(e){ return null }
   }
@@ -3247,6 +3261,11 @@ window.plausible.init=function(){};window.plausible.q=[];
       const data = await res.json();
       if(!data.url || !data.editUrl) throw new Error('Unvollstaendige Antwort vom Server');
       state.partyseite.active = true; state.partyseite.url = data.url; state.partyseite.editUrl = data.editUrl;
+      // Der Plan-Abschluss und die mitlaufende Leiste lesen beide EINLADUNG_LAEUFT(); beide werden sonst
+      // erst beim naechsten goStage(3) frisch. Wer hier aktiviert und danach nur hochscrollt, las unter
+      // seinem Plan weiter "Kinder einladen" (Re-Check 10, MINOR 2).
+      try{ syncPlanAdvance(); }catch(e){}
+      try{ if(window.__syncStickyMorph) window.__syncStickyMorph(); }catch(e){}
       /* Selbst-Catch 31.07.: Die beim Anlegen gesendeten Namen SIND der zuletzt synchronisierte
          Stand. Ohne diese Zeile haelt der Drei-Wege-Merge sie beim ersten Loeschen faelschlich
          fuer Fremd-Ergaenzungen und laesst das geloeschte Kind wieder auferstehen. */
