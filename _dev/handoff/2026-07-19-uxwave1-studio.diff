diff --git a/einladung/studio/index.html b/einladung/studio/index.html
index 1af841c..c26d7d3 100644
--- a/einladung/studio/index.html
+++ b/einladung/studio/index.html
@@ -1,30 +1,36 @@
 <!doctype html>
 <html lang="de">
 <head>
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width,initial-scale=1">
 <meta name="robots" content="noindex">
 <title>Kindergeburtstag Einladung gestalten | machsleicht.de</title>
 <meta name="description" content="Gestalte eine persönliche Kindergeburtstags-Einladung passend zu deinem Motto. Mit Foto, Partyseite, QR-Code und direktem PNG-Export.">
 <link rel="canonical" href="https://machsleicht.de/einladung/studio/">
 <style>
 :root{
-  --page:#f3f4f1; --panel:#fff; --ink:#1f2924; --muted:#6c756f; --line:#dfe4df;
+  --page:#f3f4f1; --panel:#fff; --ink:#1f2924; --muted:#5c655f; --line:#dfe4df;   /* N1: --muted von #6c756f auf ~5.4:1 (WCAG-AA) fuer 11-12px-Hints */
   --brand:#335f47; --soft:#eef4ef; --danger:#a33a36;
 }
 *{box-sizing:border-box}
 html,body{margin:0;min-height:100%;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--ink);background:var(--page)}
 button,input,select,textarea{font:inherit}
 button{cursor:pointer}
+/* H10/N3: sichtbarer Tastatur-Fokus (Inputs unterdruecken outline, hier fuer :focus-visible zurueckholen) */
+:focus-visible{outline:2px solid #2f7ee6;outline-offset:2px}
+/* H10/N3: visuell verstecktes h1 fuer Screenreader/Doc-Outline */
+.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
+/* H10: prefers-reduced-motion respektieren */
+@media(prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important;scroll-behavior:auto!important}}
 .app{min-height:100vh;display:grid;grid-template-rows:64px 1fr auto}
 .topbar{display:flex;align-items:center;justify-content:space-between;padding:0 22px;background:#fff;border-bottom:1px solid var(--line);position:sticky;top:0;z-index:50}
 .brand{display:flex;align-items:center;gap:12px;font-weight:900}
 .brandmark{width:36px;height:36px;border-radius:12px;background:linear-gradient(145deg,#436f54,#87a986);display:grid;place-items:center;color:#fff}
 .brand small{display:block;font-size:11px;font-weight:700;color:var(--muted);letter-spacing:.08em;text-transform:uppercase}
 .top-actions{display:flex;gap:8px}
 .workspace{display:grid;grid-template-columns:330px minmax(460px,1fr) 290px;min-height:0}
 .panel{background:#fff;overflow:auto;padding:18px;border-right:1px solid var(--line)}
 .panel.right{border-right:0;border-left:1px solid var(--line)}
 .section{padding:0 0 18px;margin:0 0 18px;border-bottom:1px solid var(--line)}
 .section:last-child{border:0}
 .section h3{font-size:13px;margin:0 0 10px;letter-spacing:.04em;text-transform:uppercase}
@@ -106,31 +112,31 @@ background-size:24px 24px}
 .card{
   width:420px;height:746px;position:relative;overflow:hidden;border-radius:26px;
   background:linear-gradient(160deg,var(--c1),var(--c2) 58%,var(--c3));
   box-shadow:0 18px 50px rgba(42,57,47,.18);touch-action:pan-y;user-select:none;   /* V14: Seite bleibt ueber der Karte scrollbar; Drag-Elemente haben eigenes touch-action:none */
   --c1:#e6f0d5;--c2:#c9ddb8;--c3:#f4dfad;--dark:#244f34;--accent:#cc6847;--cream:#fffaf0;
 }
 .card::after{content:"";position:absolute;inset:0;border:1px solid rgba(255,255,255,.5);border-radius:26px;pointer-events:none;z-index:100}
 .blob{position:absolute;border-radius:999px;filter:blur(.2px);opacity:.7;pointer-events:none}
 .blob.one{width:260px;height:260px;right:-105px;top:-90px;background:rgba(255,255,255,.32)}
 .blob.two{width:310px;height:230px;left:-145px;bottom:-110px;background:rgba(255,255,255,.28);transform:rotate(20deg)}
 .track{display:none}
 .design-el{position:absolute;z-index:5;touch-action:none;cursor:grab;border:1.5px solid transparent;min-width:20px;min-height:20px}
-.design-el.selected{border-color:#2f7ee6;box-shadow:0 0 0 1px #fff,0 0 0 3px rgba(47,126,230,.18)}
+.design-el.selected{border-color:#2f7ee6;box-shadow:0 0 0 1px #fff,0 0 0 3px rgba(47,126,230,.55)}   /* M3: Ring von 18% auf 55% — auf gemusterten/dunklen Karten war er kaum sichtbar */
 .design-el.editing{cursor:text;background:rgba(255,255,255,.12)}
 .design-el.text{white-space:pre-wrap;line-height:1.05;overflow:visible}
 .design-el.photo{overflow:hidden;background:#e8eee7;box-shadow:0 14px 34px rgba(28,53,37,.2);cursor:default;pointer-events:none;border-color:transparent!important}
 .design-el.photo img{width:100%;height:100%;display:block;object-fit:cover;pointer-events:none;transform-origin:center}
 .design-el.photo .empty-photo{display:none;width:100%;height:100%;place-items:center;text-align:center;color:#56665c;background:rgba(255,255,255,.74);padding:18px;font-size:13px;font-weight:850}
-.design-el.photo.empty .empty-photo{display:grid}
+.design-el.photo.empty .empty-photo{display:grid;pointer-events:auto;cursor:pointer}   /* H6: der Platzhalter sah wie ein Button aus, war aber tot — nur im Leer-Zustand klickbar */
 .design-el.photo.empty img{display:none}
 .photo-badge{position:absolute;left:15px;bottom:14px;padding:7px 11px;border-radius:999px;background:rgba(255,250,240,.9);color:var(--dark);font-size:12px;font-weight:900;box-shadow:0 5px 15px rgba(0,0,0,.1);pointer-events:none}
 .info-panel{position:absolute;left:24px;right:24px;bottom:18px;height:232px;border-radius:24px;background:rgba(255,250,240,.18);border:1px solid rgba(255,255,255,.24);box-shadow:0 8px 24px rgba(47,62,50,.045);backdrop-filter:blur(2px);z-index:2;transition:background .18s ease,box-shadow .18s ease,border-color .18s ease,backdrop-filter .18s ease}
 .info-panel::before{content:"";position:absolute;left:20px;right:20px;top:62px;height:1px;background:rgba(36,79,52,.08);opacity:var(--line-opacity,1);transition:opacity .18s ease}
 .resize-handle{position:absolute;width:14px;height:14px;border-radius:50%;right:-8px;bottom:-8px;background:#2f7ee6;border:2px solid #fff;display:none;z-index:20;cursor:nwse-resize}
 .design-el.selected .resize-handle{display:block}
 .design-el.photo .resize-handle{display:none!important}
 .design-el.photo.selected{border-color:transparent!important;box-shadow:0 14px 34px rgba(28,53,37,.2)!important}
 .quick-stepper{display:grid;grid-template-columns:42px 1fr 42px;gap:7px;align-items:center}
 .quick-stepper button{height:40px;border:1px solid var(--line);background:#fff;border-radius:10px;font-size:21px;font-weight:900}
 .quick-value{text-align:center;font-size:13px;font-weight:900;padding:10px 4px;border-radius:10px;background:#f5f7f5}
 .opacity-options{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}
@@ -194,58 +200,60 @@ body.autopilot .ap-bar{display:flex;flex-direction:column;gap:10px;align-items:c
   .panel.right{display:none}
 }
 @media(max-width:760px){
   .site-footer{padding-bottom:92px}
   .back-planer{font-size:0}.back-planer::before{content:"←";font-size:18px}
   .topbar-left{gap:7px}
 
   html,body{overscroll-behavior-y:none}
   body{padding-bottom:76px}
   .app{grid-template-rows:56px auto;min-height:100dvh}
   .topbar{padding:0 10px;height:56px}
   .brand{gap:8px;font-size:14px}.brandmark{width:32px;height:32px;border-radius:10px}.brand small{display:none}
-  .top-actions{gap:6px}.top-actions .btn{min-height:40px;padding:8px 11px}.top-actions .btn:not(.primary){display:none}
+  .top-actions{gap:6px}.top-actions .btn{min-height:40px;padding:8px 11px}.top-actions .btn:not(#shareBtn){display:none}
+  .top-actions #shareBtn{display:inline-flex;align-items:center;background:var(--brand);color:#fff;border-color:var(--brand)}  /* K1: Teilen ist die laute Topbar-Aktion auf Mobil (nicht der technische PNG-Export; das Share-Sheet deckt "speichern" mit ab) */
   .workspace{display:flex;flex-direction:column;min-width:0}
   .stage{order:1;padding:10px 5px 16px;overflow:visible;background-size:18px 18px}
   .stage-inner{width:100%;gap:8px}
   .stage-toolbar{position:sticky;top:58px;z-index:35;width:max-content;max-width:calc(100vw - 16px);padding:6px;border-radius:14px;background:rgba(255,255,255,.92);box-shadow:0 6px 20px rgba(33,51,40,.1);backdrop-filter:blur(10px)}
   .stage-toolbar .btn{min-height:38px;padding:7px 10px;font-size:12px}
   .stage-toolbar #clearBtn{display:none}
   .card-shell{padding:5px;border-radius:22px;max-width:100%}
   .card{width:420px;height:746px;border-radius:22px;transform-origin:top left}   /* V14: intern immer 420x746, JS skaliert per transform (Elemente in 420er-Koordinaten wurden sonst abgeschnitten) */
   .card::after{border-radius:22px}
   .hint{display:none}
   .panel:not(.right){display:none;position:fixed;left:8px;right:8px;bottom:78px;z-index:160;max-height:55dvh;overflow:auto;border:1px solid rgba(60,79,67,.14);border-radius:20px;background:#fff;padding:10px;box-shadow:0 20px 60px rgba(26,44,32,.24)}
   body.mobile-panel-open .panel:not(.right){display:block}
   .panel:not(.right) .section{display:none;margin:0;padding:12px;border:0;border-radius:15px;background:#fff;box-shadow:none}
   .panel:not(.right) .section.mobile-active{display:block}
   .panel:not(.right) .section h3{font-size:12px}
   .theme-grid{max-height:none;grid-template-columns:repeat(3,1fr);gap:7px}
   .theme-card{min-height:64px;padding:7px 4px}.theme-card span{font-size:22px}
   .layout-grid{grid-template-columns:repeat(3,1fr)}
   input,select,textarea{font-size:16px;min-height:44px}
   .btn{min-height:44px}
   .emoji-list{grid-template-columns:repeat(6,1fr)}
   .emoji-btn{min-height:44px;font-size:23px}
   .upload{min-height:78px}
-  .design-el.selected{border-width:2px;box-shadow:0 0 0 2px #fff,0 0 0 5px rgba(47,126,230,.2)}
+  .design-el.selected{border-width:2px;box-shadow:0 0 0 2px #fff,0 0 0 5px rgba(47,126,230,.6)}   /* M3 mobil */
   .resize-handle{width:22px;height:22px;right:-12px;bottom:-12px;border-width:3px}
+  .resize-handle::after{content:"";position:absolute;inset:-11px}   /* M2: sichtbarer Punkt bleibt 22px, Trefferflaeche ~44px */
   .panel.right{display:none;position:fixed;left:8px;right:8px;bottom:78px;z-index:180;border:1px solid rgba(60,79,67,.14);border-radius:20px;background:#fff;padding:0;max-height:48dvh;overflow:auto;box-shadow:0 20px 60px rgba(26,44,32,.28)}
   body.selection-open .panel.right{display:block}
   .panel.right .section{display:none;border:0;margin:0;padding:14px 14px 16px}
   .panel.right>.section:first-of-type{display:block}   /* Bugfix 18.07.: erstes Kind ist .mobile-editor-head (DIV) -> :first-child matchte KEINE Section -> mobiles Auswahl-Panel (Textfeld+Loeschen) war komplett leer. :first-of-type trifft die erste <section>. */
   .panel.right>.section:first-of-type>h3{display:none}
   .panel.right #deleteBtn{display:none}   /* Mobil: Loeschen sitzt im sticky Header (#mobileHeadDelete) -> der Panel-End-Button waere ein zweiter Loesch-Trigger (Review-Fix) */
   .mobile-editor-head{display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;background:#fff;padding:12px 14px 8px;border-bottom:1px solid var(--line);z-index:3;font-size:13px;font-weight:900}
-  .mobile-close-editor{display:grid;width:34px;height:34px;place-items:center;border:0;border-radius:10px;background:#f0f3f0;font-size:18px}
+  .mobile-close-editor{display:grid;width:44px;height:44px;place-items:center;border:0;border-radius:12px;background:#f0f3f0;font-size:18px}   /* M2: >=44px Trefferflaeche */
   .mobile-nav{display:grid;grid-template-columns:repeat(5,1fr);position:fixed;left:0;right:0;bottom:0;z-index:220;padding:7px 6px calc(7px + env(safe-area-inset-bottom));background:rgba(255,255,255,.96);border-top:1px solid var(--line);box-shadow:0 -8px 30px rgba(30,48,36,.12);backdrop-filter:blur(12px)}
   .mobile-nav button{border:0;background:transparent;border-radius:12px;padding:6px 2px;color:#6b756e;font-size:10px;font-weight:800;min-height:51px}
   .mobile-nav button span{display:block;font-size:20px;line-height:1.15;margin-bottom:2px}
   .mobile-nav button.active{background:var(--soft);color:var(--brand)}
   .mobile-only{display:inline-flex}
   .share-toast{bottom:86px}
 }
 @media(max-width:370px){
   .stage-toolbar .btn{padding:6px 8px;font-size:11px}
   .theme-grid{grid-template-columns:repeat(3,1fr)}
   .mobile-nav button{font-size:9px}
 }
@@ -268,70 +276,71 @@ body.autopilot .ap-bar{display:flex;flex-direction:column;gap:10px;align-items:c
 .ap-btn[disabled]{opacity:.5;pointer-events:none}  /* A5-1: bis apStart die Karte gefuellt hat */
 
 </style>
 <!-- Privacy-friendly analytics by Umami + plausible()->umami Shim (Site-Konvention) -->
 <script defer src="https://cloud.umami.is/script.js" data-website-id="72b5eb12-dfde-4333-9bc7-0c2880864df2"></script>
 <script>
 window.plausible=function(name,opts){if(window.umami){try{umami.track(name,(opts&&opts.props)||{})}catch(e){}}};
 window.plausible.init=function(){};window.plausible.q=[];
 </script>
 </head>
 <body>
 <div class="app">
+<h1 class="sr-only">Kindergeburtstags-Einladung gestalten</h1>
 <header class="topbar">
   <div class="topbar-left">
     <a class="back-planer" href="/kindergeburtstag.html">← Zurück zum Planer</a>
     <div class="brand"><div class="brandmark">m</div><div>Einladung Studio<small>machsleicht.de</small></div></div>
   </div>
   <div class="top-actions">
     <button class="btn" id="reloadPlannerBtn">Neu aus Planer laden</button>
     <button class="btn" id="saveBtn">Entwurf speichern</button>
     <button class="btn" id="resetBtn">Neu aufbauen</button>
     <button class="btn" id="shareBtn">Teilen</button>
-    <button class="btn primary" id="exportBtn">PNG exportieren</button>
+    <button class="btn primary" id="exportBtn">Als Bild speichern</button>
   </div>
 </header>
 
 <div class="workspace">
 <aside class="panel">
   <div class="planner-notice" id="plannerNotice" hidden></div>
   <section class="section">
     <h3>Motto</h3>
     <div class="theme-grid" id="themeGrid"></div>
   </section>
 
   <section class="section">
     <h3>Design</h3>
     <div class="section-note">Das erste Design bleibt der Standard. Die anderen Varianten ordnen dieselben Inhalte automatisch neu an.</div>
     <div class="layout-grid" id="layoutGrid">
       <button class="layout-card active" data-layout="classic"><span>▣</span>Klassisch</button>
       <button class="layout-card" data-layout="photo"><span>▤</span>Foto-Fokus</button>
       <button class="layout-card" data-layout="clean"><span>◫</span>Clean</button>
     </div>
   </section>
 
   <section class="section">
     <h3>Party Daten</h3>
     <div class="grid2">
-      <div class="field"><label>Name</label><input id="kidName" value="Matti"></div>
+      <div class="field"><label>Name</label><input id="kidName" value="Matti" maxlength="18"></div>
       <div class="field"><label>Alter</label><input id="kidAge" type="number" value="6" min="1" max="16"></div>
     </div>
     <div class="grid2">
       <div class="field"><label>Datum</label><input id="partyDate" value="20. Juli 2026"></div>
       <div class="field"><label>Uhrzeit</label><input id="partyTime" value="14:00 Uhr"></div>
     </div>
     <div class="field"><label>Ort</label><input id="partyPlace" value="Hamburg"></div>
-    <div class="field"><label>Einladungstext</label><textarea id="partyMessage">Professor Rex braucht deine Hilfe bei einer geheimen Dino Mission.</textarea></div>
+    <div class="field"><label>Einladungstext</label><textarea id="partyMessage" maxlength="140">Professor Rex braucht deine Hilfe bei einer geheimen Dino Mission.</textarea></div>
     <div class="field"><label>Link zur Partyseite</label><input id="partyUrl" value="" placeholder="https://party.machsleicht.de/…"><div class="section-note" style="margin:5px 0 0">Erscheint nur, wenn eine echte Partyseite vorhanden ist.</div><div class="party-activation-hint" id="partyActivationHint">Partyseite im Planer aktivieren — dann kommen Zusagen automatisch. <a href="/kindergeburtstag.html">Zum Planer</a></div></div>
-    <button class="btn soft full" id="applyDataBtn">Daten auf Einladung übernehmen</button>
+    <div class="section-note" style="margin:2px 0 0">Änderungen erscheinen sofort auf der Einladung.</div>
   </section>
 
   <section class="section">
     <h3>Bild auf der Einladung</h3>
     <div class="photo-mode-switch">
       <button type="button" id="withPhotoBtn" class="active">📷 Mit Foto</button>
       <button type="button" id="withoutPhotoBtn">✨ Ohne Foto</button>
     </div>
     <div class="section-note" id="photoModeNote">Das Foto sitzt automatisch sauber im Layout und muss nicht bearbeitet werden.</div>
     <div class="photo-upload-controls" id="photoUploadControls">
       <label class="upload" for="photoInput">
         <img class="upload-preview" id="uploadPreview" alt="">
@@ -446,25 +455,25 @@ window.plausible.init=function(){};window.plausible.q=[];
     </div>
   </section>
 </aside>
 </div>
 
 <nav class="mobile-nav" id="mobileNav" aria-label="Editor Navigation">
   <button type="button" data-mobile-tab="motto"><span>🎨</span>Motto</button>
   <button type="button" data-mobile-tab="daten"><span>✏️</span>Daten</button>
   <button type="button" data-mobile-tab="foto"><span>📷</span>Foto</button>
   <button type="button" data-mobile-tab="elemente"><span>✨</span>Extras</button>
   <button type="button" data-mobile-tab="preview" class="active"><span>👁️</span>Vorschau</button>
 </nav>
-<div class="share-toast" id="shareToast">Fertig</div>
+<div class="share-toast" id="shareToast" role="status" aria-live="polite">Fertig</div>
 <footer class="site-footer"><a href="/impressum.html">Impressum</a><a href="/datenschutz.html">Datenschutz</a></footer>
 </div>
 
 <script>
 (() => {
 let demoPhotoData = "";
 const $ = id => document.getElementById(id);
 const card = $("card");
 let selected = null;
 let zTop = 30;
 let history = [];
 let _sharing = false;  // A6-6: hierher gezogen (war 1350 Z. tiefer) — die _sharing-Guards in applyLayout/applyTheme/... liegen VOR der alten Deklaration; kuenftiger Top-Level-Aufruf haette sonst ReferenceError geworfen
@@ -620,24 +629,32 @@ const commonStickers=["🎈","🎁","🎉","🧁","⭐","✨","❤️","📍","
 
 function canonicalThemeId(value){
   const id=String(value||"").trim().toLowerCase();
   if(id==="pirat")return "piraten";
   return themes[id]?id:"";
 }
 
 function themeMessage(themeId,name){
   const template=(themes[themeId]||themes.dino).message||"";
   return template.replaceAll("{name}",(name||"Geburtstagskind").trim()||"Geburtstagskind");
 }
 
+// M7: leerer Name/Alter darf keine Fragment-Headline (" WIRD 6!") ins geteilte PNG schreiben
+function headlineText(){
+  const n=($("kidName").value||"").trim().toUpperCase();
+  const a=($("kidAge").value||"").trim();
+  if(!n)return "WIR FEIERN!";
+  return a?`${n} WIRD ${a}!`:`${n} FEIERT!`;
+}
+
 function track(name,props={}){
   try{
     if(typeof window.plausible==="function")window.plausible(name,{props});
   }catch{}
 }
 
 function renderEmojiList(){
   const root=$("emojiList");
   root.innerHTML="";
   const list=[...new Set([...(themes[currentTheme].stickers||[]),...commonStickers])].slice(0,18);
   list.forEach(e=>{
     const b=document.createElement("button");
@@ -958,25 +975,25 @@ function addQrPlaceholder(){
   return el;
 }
 
 function buildDesign(){
   clearDesign(false);
   const t=themes[currentTheme];
 
   const mission=addText(t.mission.toUpperCase(),"mission",34,28,352,13,900,t.dark,"center","dark");
   mission.dataset.autoTheme="true";
   mission.style.letterSpacing=".16em";
   fitSingleLine(mission,9,14);
 
-  const headline=addText(`${$("kidName").value.toUpperCase()} WIRD ${$("kidAge").value}!`,"headline",28,57,364,40,950,t.dark,"center","dark");
+  const headline=addText(headlineText(),"headline",28,57,364,40,950,t.dark,"center","dark");
   fitSingleLine(headline,22,42);  // A5-4: Minimum an applyLayout angleichen (22, nicht 25) — sonst ueberlaeuft ein langer Name im Nicht-Autopilot-Prefill
 
   const invite=addText("Du bist eingeladen!","invite",44,468,332,28,900,t.dark,"center","dark");
   fitSingleLine(invite,17,30);  // A5-4: 17 statt 20 (= applyLayout)
 
   addPhoto();
   addHeroMotif();
 
   const teamBadge=addText(t.badge,"teamBadge",50,417,164,11,900,t.dark,"center","dark");
   teamBadge.dataset.label="Team Bezeichnung";
   teamBadge.dataset.autoTheme="true";
   Object.assign(teamBadge.style,{
@@ -1130,25 +1147,25 @@ document.querySelectorAll(".layout-card").forEach(btn=>{
 });
 
 function clearDesign(record=true){
   if(record)snapshot();
   card.querySelectorAll(".design-el").forEach(e=>e.remove());
   selected=null; updateSelectionUI(); updateLayers();
 }
 
 function applyData(record=true){
   if(_sharing&&record)return;  // A5-5
   if(record)snapshot();   // V14: Live-Tippen (partyUrl) darf die 40er-Undo-History nicht fluten
   const map={
-    headline:`${$("kidName").value.toUpperCase()} WIRD ${$("kidAge").value}!`,
+    headline:headlineText(),
     date:`📅  ${$("partyDate").value}`,
     time:`⏰  ${$("partyTime").value}`,
     place:`📍  ${$("partyPlace").value}`,
     message:$("partyMessage").value,
     partyLink:displayPartyUrl()
   };
   Object.entries(map).forEach(([k,v])=>{
     const el=card.querySelector(`[data-key="${k}"]`);
     if(!el)return;
     setText(el,v);
     const _cl=currentLayout==='clean';   // V14: applyData nutzte Classic-Grenzen — Clean wuchs beim Datenwechsel wieder gross
     if(k==="headline")fitSingleLine(el,22,_cl?36:42);
@@ -1389,24 +1406,25 @@ document.querySelectorAll("#opacityOptions button").forEach(btn=>{
   };
 });
 
 function setColor(v){if(selected&&selected.dataset.type!=="photo"){selected.style.color=v;$("elementColor").value=v;$("elementColorText").value=v}}
 $("elementColor").addEventListener("input",e=>setColor(e.target.value));
 $("elementColorText").addEventListener("change",e=>setColor(e.target.value));
 $("frontBtn").onclick=()=>{if(selected){snapshot();selected.style.zIndex=++zTop;updateLayers()}};
 $("backBtn").onclick=()=>{if(selected){snapshot();selected.style.zIndex=Math.max(3,(+selected.style.zIndex||5)-1);updateLayers()}};
 $("deleteBtn").onclick=deleteSelected;
 
 function deleteSelected(){
   if(!selected)return; snapshot(); selected.remove(); selected=null; updateSelectionUI(); updateLayers();
+  toast("Gelöscht — ↶ Rückgängig oben");   // K4: Löschen war stumm; Undo-Pfad sichtbar machen
 }
 function duplicateSelected(){
   if(!selected)return; snapshot();
   const clone=selected.cloneNode(true);
   clone.classList.remove("selected","editing");
   clone.dataset.bound="";   // V14: cloneNode kopiert data-bound="1" -> bindElement brach sofort ab, Duplikat war tot
   clone.style.left=(parseFloat(selected.style.left)+16)+"px"; clone.style.top=(parseFloat(selected.style.top)+16)+"px";
   clone.style.zIndex=++zTop; card.appendChild(clone); bindElement(clone); selectElement(clone); updateLayers();
 }
 
 function startEdit(el){   // Feinschliff 18.07.: geteilte Editier-Logik fuer dblclick UND Tap-auf-selektierte-Karte (Touch, wo dblclick unzuverlaessig ist)
   el.classList.add("editing");el.contentEditable="true";el.focus();
@@ -1538,46 +1556,58 @@ function rebuildDesign(items){
     if(it.cta)el.classList.add('party-cta');
     if(it.hero)el.classList.add('hero-motif');
     STYLE_KEYS.forEach(k=>{const v=it.style&&it.style[k];if(v!==undefined&&v!==null&&v!=='')el.style[k]=String(v)});
   });
   const zs=[...card.querySelectorAll('.design-el')].map(e=>parseInt(e.style.zIndex)||0);
   zTop=Math.max(30,...(zs.length?zs:[30]));
   setPhoto(currentPhoto);
   updateSelectionUI();updateLayers();
 }
 function snapshot(){
   history.push({design:serializeDesign(),theme:currentTheme,photo:currentPhoto});
   if(history.length>40)history.shift();
+  scheduleAutosave();   // K4: jede Mutation debounced persistieren — kein stiller Datenverlust bei Reload/Anruf/Tab-Wechsel
+}
+// K4: Autosave. Nicht im Share-Blob-Lauf (Misch-State) und nicht im Autopilot (Karte evtl. noch leer/design:[] -> wuerde echten Entwurf clobbern).
+let _autosaveTimer=0;
+function scheduleAutosave(){
+  clearTimeout(_autosaveTimer);
+  _autosaveTimer=setTimeout(()=>{
+    if(_sharing||document.body.classList.contains("autopilot"))return;
+    if(!saveStudioState(false))toast("Automatisches Speichern fehlgeschlagen — evtl. ist der Browser-Speicher voll",true);
+  },1000);
 }
 function undo(){
   if(_sharing)return;  // W15a: kein State-Wechsel waehrend des Share-Blob-Laufs (Misch-PNG)
   const state=history.pop();if(!state)return;
   currentPhoto=state.photo;
   rebuildDesign(state.design);
   applyTheme(state.theme,false);
+  scheduleAutosave();   // K4: auch der zurückgenommene Stand wird persistiert
 }
 function updateLayers(){
   const root=$("layers");root.innerHTML="";
   [...card.querySelectorAll(".design-el")].filter(el=>el.dataset.type!=="photo").sort((a,b)=>(+b.style.zIndex||0)-(+a.style.zIndex||0)).forEach(el=>{
     const row=document.createElement("div");row.className="layer"+(el===selected?" active":"");
     const text=(el.dataset.label||el.dataset.key||el.dataset.type)+"";
     const icon=el.dataset.type==="qr"?"▦":el.dataset.type==="photo"?"📷":el.dataset.key==="heroMotif"?"◎":el.dataset.type==="emoji"?"✨":"T";
     const span=document.createElement("span");span.textContent=icon+"  "+text.slice(0,23);   // V14: textContent statt innerHTML (Label stammt aus gespeichertem State)
-    const btn=document.createElement("button");btn.title="Auswählen";btn.textContent="◎";
+    const btn=document.createElement("button");btn.title="Auswählen: "+text;btn.setAttribute("aria-label","Auswählen: "+text);btn.textContent="◎";   // H10: jeder Ebenen-Button hiess "Auswählen" — jetzt unterscheidbar fuer Screenreader
     row.append(span,btn);
     row.onclick=()=>selectElement(el);root.appendChild(row);
   });
 }
 function clearSelection(){selectElement(null)}
 card.addEventListener("pointerdown",e=>{if(e.target===card||e.target.classList.contains("blob")||e.target.classList.contains("info-panel"))clearSelection()});
+card.addEventListener("click",e=>{if(e.target.closest(".empty-photo"))$("photoInput").click()});   // H6: leerer Foto-Platzhalter oeffnet den Datei-Dialog (bindElement steigt fuer Fotos aus)
 
 // V14: Mobile-Skalierung der ganzen Karte per transform — Drag-Mathe bleibt korrekt (scale=420/rect.width).
 function fitCardToViewport(){
   const shell=document.querySelector('.card-shell');
   if(!shell)return;
   if(document.body.classList.contains('autopilot')){
     // Autopilot: Karte muss KOMPLETT zwischen Kopf und fixer Aktionsleiste passen (Desktop wie Mobile).
     // Reale Shell-Oberkante messen statt Pauschalen — Paddings/Margins der Stage variieren per Breakpoint.
     const stage=document.querySelector('main.stage');if(stage)stage.scrollTop=0;
     // A3-2: barH nur abziehen, wenn die Bar wirklich fixed ist — im Landscape-Modus (max-height:480px) liegt sie im Fluss
     const _bar=document.getElementById('apBar');
     const barH=(_bar&&getComputedStyle(_bar).position==='fixed')?(_bar.offsetHeight||0):0;
@@ -1702,25 +1732,25 @@ function applyPlannerState(state,{showToast=true}={}){
   if(showToast)toast('Deine Party-Daten sind übernommen ✓');
   return true;
 }
 
 function getFormData(){
   return {kidName:$('kidName').value,kidAge:$('kidAge').value,partyDate:$('partyDate').value,partyTime:$('partyTime').value,partyPlace:$('partyPlace').value,partyMessage:$('partyMessage').value,partyUrl:$('partyUrl').value};
 }
 
 function saveStudioState(showToast=true){
   try{
     // Foto liegt genau EINMAL im State (photo-Feld); die Elementliste ist reine Struktur ohne dataURLs.
     localStorage.setItem(STUDIO_KEY,JSON.stringify({v:2,plannerFp:plannerFingerprint(readPlannerState()),design:serializeDesign(),theme:currentTheme,layout:currentLayout,photoMode,photo:currentPhoto,panelMode,form:getFormData()}));
-    if(showToast)toast('Entwurf lokal gespeichert ✓');
+    if(showToast)toast('Entwurf gespeichert ✓');
     return true;
   }catch(error){
     if(showToast)toast('Entwurf konnte nicht gespeichert werden',true);  // A4-4: der eine Toast, auf den reagiert werden MUSS
     return false;
   }
 }
 
 function restoreStudioState(saved){
   if(!saved||typeof saved!=='object')return false;
   if(!Array.isArray(saved.design)||!saved.design.length)return false;   // altes html-Format oder leer -> Prefill/Demo
   try{
     currentTheme=canonicalThemeId(saved.theme)||'dino';currentLayout=saved.layout||'classic';photoMode=saved.photoMode||'with';panelMode=saved.panelMode||'glass';currentPhoto=saved.photo||demoPhotoData;
@@ -1742,24 +1772,27 @@ function updatePlannerNotice(state,imported=false){
     notice.hidden=false;
     notice.innerHTML=`<strong>${imported?'Party-Daten übernommen ✓':'Party-Daten im Planer gefunden'}</strong><span>${imported?'Die Einladung ist fertig befüllt.':'Du kannst den Studio-Entwurf bewusst mit den aktuellen Planer-Daten ersetzen.'}</span><button class="btn soft" type="button" id="reloadPlannerInline">Neu aus Planer laden</button>`;
     notice.querySelector('#reloadPlannerInline').onclick=reloadFromPlanner;
   }else{
     notice.hidden=false;
     notice.innerHTML='<strong>Demo-Modus</strong><span>Noch keine Party geplant? </span><a href="/kindergeburtstag.html">Erst den Party-Planer ausfüllen →</a>';
   }
 }
 
 function reloadFromPlanner(){
   const state=readPlannerState();
   if(!plannerStateReady(state)){toast('Keine vollständigen Planer-Daten gefunden');updatePlannerNotice(state,false);return}
+  // K4: der scharfe Button loeschte den Entwurf ohne Rueckfrage. Nur nachfragen, wenn wirklich ein Entwurf zu verlieren ist.
+  const existing=readStudioState();
+  if(existing&&Array.isArray(existing.design)&&existing.design.length&&!confirm('Deinen aktuellen Entwurf verwerfen und frisch aus dem Planer laden?'))return;
   localStorage.removeItem(STUDIO_KEY);
   applyPlannerState(state,{showToast:true});
   saveStudioState(false);
 }
 
 function setupMobilePanels(){
   const sections=[...document.querySelectorAll('.panel:not(.right) .section')];
   const groups={
     motto:["Motto","Design"],
     daten:["Party Daten"],
     foto:["Bild auf der Einladung"],
     elemente:["Datenbereich","Element hinzufügen"]
@@ -1788,27 +1821,33 @@ function setMobileTab(tab){
 $("mobileCloseEditor").onclick=clearSelection;
 $("mobileHeadDelete").onclick=deleteSelected;   // Feinschliff 18.07.: Loeschen immer im sticky Mobil-Header erreichbar (der andere sitzt am Panel-Ende, unter dem Fold)
 
 function toast(text,long){
   const el=$("shareToast");
   // A5-6: im Autopilot ueber die fixe ap-bar heben (sonst liegt der Toast auf den Chips)
   const _bar=$('apBar');
   el.style.bottom=(document.body.classList.contains('autopilot')&&_bar&&getComputedStyle(_bar).position==='fixed')?(_bar.offsetHeight+12)+'px':'';
   el.textContent=text;el.classList.add("show");
   clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.classList.remove("show"),(long||text.length>40)?3200:1600);  // A3-5/A4-4: Fehler + Handlungsaufforderungen brauchen Lesezeit
 }
 
-$("partyMessage").addEventListener("input",()=>{$("partyMessage").dataset.manual="true"});
-$("partyUrl").addEventListener("input",()=>{applyData(false);syncPartyArea()});
-$("applyDataBtn").onclick=()=>{applyData();toast("Einladung aktualisiert ✓")};
+// K3: alle Datenfelder binden jetzt LIVE an die Karte (vorher nur partyUrl) — die erste Bearbeitung wirkte sonst kaputt.
+// scheduleAutosave (K4) persistiert die Eingabe debounced, damit Tab-Wechsel/Reload sie nicht verliert.
+["kidAge","partyDate","partyTime","partyPlace"].forEach(id=>$(id).addEventListener("input",()=>{applyData(false);scheduleAutosave()}));
+$("kidName").addEventListener("input",()=>{
+  if($("partyMessage").dataset.manual!=="true"){$("partyMessage").value=themeMessage(currentTheme,$("kidName").value)}
+  applyData(false);scheduleAutosave();
+});
+$("partyMessage").addEventListener("input",()=>{$("partyMessage").dataset.manual="true";applyData(false);scheduleAutosave()});
+$("partyUrl").addEventListener("input",()=>{applyData(false);syncPartyArea();scheduleAutosave()});
 $("addTextBtn").onclick=addCustomText;
 $("addEmojiBtn").onclick=()=>chooseEmoji("✨");
 $("duplicateBtn").onclick=duplicateSelected;
 $("clearBtn").onclick=clearSelection;
 $("undoBtn").onclick=undo;
 $("resetBtn").onclick=()=>{if(_sharing)return;snapshot();buildDesign();toast("Einladung neu aufgebaut")};  // W15a
 $("saveBtn").onclick=()=>saveStudioState(true);
 $("reloadPlannerBtn").onclick=reloadFromPlanner;
 
 document.addEventListener("keydown",e=>{
   if(_sharing)return;  // W15a: Pfeiltasten/Delete auf ein selektiertes Element waehrend des Share-Blob-Laufs gaeben ein Misch-PNG
   if(!selected||selected.classList.contains("editing"))return;
@@ -1988,55 +2027,101 @@ async function renderInvitationCanvas(scale=3){
 async function renderInvitationBlob(){
   clearSelection();
   // A5-2 (ersetzt A4-1): nur schrumpfen — die Live-Karte ist bereits Layout-korrekt gefittet (apApply/applyLayout),
   // manuelle Groessen bleiben erhalten, das PNG bildet exakt die sichtbare Karte ab (WYSIWYG).
   // W15b zurueckgerollt (Review 17.07.): die _ov-Snapshot-Bedingung mass Overflow im pre-wrap-Modus, fitDownToWidth
   // schrumpft aber im nowrap-Modus -> Bedingung feuerte im Normalfall nicht = wirkungsloser Undo-Punkt. Die eigentliche
   // Baustelle ist fitDownToWidths unbedingte nowrap/fontSize-Mutation (pre-existing, live) -> separates Playtest-Ticket.
   fitDownToWidth(card.querySelector('[data-key="headline"]'),22);fitDownToWidth(card.querySelector('[data-key="mission"]'),9);fitDownToWidth(card.querySelector('[data-key="invite"]'),17);
   const canvas=await renderInvitationCanvas(3);
   return await new Promise((resolve,reject)=>canvas.toBlob(blob=>blob?resolve(blob):reject(new Error('PNG konnte nicht erzeugt werden')),'image/png',1));
 }
 
+// K5: teilbarer Rueckkanal — RSVP-Link + Name/Datum + Marken-Signatur in den Share-Text (vorher nur "Du bist eingeladen!")
+function buildShareText(){
+  const n=($('kidName').value||'').trim();
+  const a=($('kidAge').value||'').trim();
+  const d=($('partyDate').value||'').trim();
+  const url=normalizedPartyUrl();
+  let head;
+  if(n&&a)head=`${n} wird ${a}! Du bist herzlich eingeladen.`;
+  else if(n)head=`${n} feiert Geburtstag! Du bist herzlich eingeladen.`;
+  else head='Du bist herzlich eingeladen!';
+  const lines=[head];
+  if(d)lines.push('📅 '+d);
+  if(url)lines.push('Zusagen & Infos: '+url);
+  lines.push('— erstellt mit machsleicht.de');
+  return lines.join('\n');
+}
+
+// K2: die Kalt-Start-Demo (Matti/Hamburg, kein Planer) darf nicht als echte Einladung geteilt/exportiert werden.
+// Keyt bewusst auf "Form == komplette Demo-Defaults UND keine Planer-Daten" (nicht nur "keine Planer-Daten"),
+// damit ein Elternteil, das auf frischem Geraet SEINE Daten eintippt, NICHT faelschlich blockiert wird.
+function looksLikeUntouchedDemo(){
+  const f=getFormData();
+  return f.kidName.trim()==='Matti'&&String(f.kidAge).trim()==='6'&&f.partyPlace.trim()==='Hamburg'
+    &&f.partyDate.trim()==='20. Juli 2026'&&!plannerStateReady(readPlannerState());
+}
+function guardDemoShare(){
+  if(!looksLikeUntouchedDemo())return false;
+  toast('Das ist noch die Beispiel-Einladung. Trag zuerst den Namen deines Kindes ein.',true);
+  if(window.matchMedia('(max-width:760px)').matches)setMobileTab('daten');
+  const n=$('kidName');if(n){try{n.focus();n.select&&n.select();}catch(e){}}
+  return true;
+}
+
 async function exportPNG(){
-  if(_sharing)return;_sharing=true;  // A4-2: Export+Share teilen sich EIN Render-Flag — parallele Canvas-Laeufe/Toast-Konkurrenz
+  if(_sharing)return;
+  if(guardDemoShare())return;   // K2
+  _sharing=true;  // A4-2: Export+Share teilen sich EIN Render-Flag — parallele Canvas-Laeufe/Toast-Konkurrenz
   // A6-4: gleicher Sperrumfang wie shareInvitation — sonst bleiben Share/Chips/Exit waehrend des Blob-Laufs optisch aktiv (Misch-PNG-Risiko + Screenreader ohne Feedback)
   const _sb=[$('shareBtn'),$('mobileShareBtn'),$('exportBtn'),$('apShareBtn'),$('apEditBtn'),$('apChips')].filter(Boolean);_sb.forEach(b=>{b.classList.add('export-busy');if(b.tagName==='BUTTON')b.setAttribute('aria-disabled','true');});
   if(card)card.style.pointerEvents='none';  // A6-2: Element-Drag/Selektion waehrend des async-Blob-Laufs unterbinden
   try{
-    toast('PNG wird erstellt …');
+    toast('Bild wird erstellt …');
     const blob=await renderInvitationBlob(),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${$('kidName').value||'einladung'}-${currentTheme}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1200);
-    track('studio_export');toast('PNG gespeichert ✓');
-  }catch(error){alert('PNG konnte nicht erstellt werden: '+error.message)}finally{_sharing=false;if(card)card.style.pointerEvents='';_sb.forEach(b=>{b.classList.remove('export-busy');if(b.tagName==='BUTTON')b.removeAttribute('aria-disabled');});}
+    track('studio_export');toast('Bild gespeichert ✓');
+  }catch(error){toast('Das Bild ließ sich gerade nicht erstellen. Bitte versuch es nochmal.',true)/* H8: war rohes alert(error.message) */}finally{_sharing=false;if(card)card.style.pointerEvents='';_sb.forEach(b=>{b.classList.remove('export-busy');if(b.tagName==='BUTTON')b.removeAttribute('aria-disabled');});}
 }
 
 // A2-2: _sharing verhindert Doppel-Tap-Blob-Laeufe (Deklaration jetzt oben im State-Block, A6-6)
 async function shareInvitation(){
-  if(_sharing)return;_sharing=true;
+  if(_sharing)return;
+  if(guardDemoShare())return;   // K2: Beispiel-Einladung nicht teilbar
+  _sharing=true;
   const _src=apActive?'autopilot':'editor';  // A3-4: Quelle VOR dem async-Blob-Lauf einfrieren (Exit waehrenddessen kippte das Prop)
   const _sb=[$('shareBtn'),$('mobileShareBtn'),$('exportBtn'),$('apShareBtn'),$('apEditBtn'),$('apChips')].filter(Boolean);_sb.forEach(b=>{b.classList.add('export-busy');if(b.tagName==='BUTTON')b.setAttribute('aria-disabled','true');});  // A3-4/A4-2/A5-8: Chips+Exit+Export sperren (Misch-PNG); aria-disabled fuer SR
   if(card)card.style.pointerEvents='none';  // A6-2: Element-Drag/Selektion waehrend des async-Blob-Laufs unterbinden (paintCard liest live-DOM nach fonts.ready/loadImage)
   try{
     toast('Einladung wird vorbereitet …');const blob=await renderInvitationBlob();const file=new File([blob],`${$('kidName').value||'einladung'}-${currentTheme}.png`,{type:'image/png'});
-    const downloadFallback=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=file.name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1200);track('studio_share',{source:_src});toast('PNG gespeichert ✓')};
+    const downloadFallback=()=>{
+      const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=file.name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1200);
+      track('studio_share',{source:_src});
+      // K1: nicht mehr stumm — Web-Share fehlt (Desktop) -> erklaeren + RSVP-Link in die Zwischenablage
+      const _purl=normalizedPartyUrl();
+      if(_purl&&navigator.clipboard&&navigator.clipboard.writeText){try{navigator.clipboard.writeText(_purl)}catch(e){}}
+      toast(_purl?'Bild gespeichert — jetzt in WhatsApp anhängen. Der Zusage-Link ist kopiert.':'Bild gespeichert — jetzt in WhatsApp/Signal anhängen und verschicken.',true);
+    };
     if(navigator.share&&(!navigator.canShare||navigator.canShare({files:[file]}))){
       // W15c zurueckgerollt (Review 17.07.): navigator.share ist pending-BY-DESIGN, solange der Share-Sheet offen ist.
       // Der 30s-Watchdog feuerte deshalb bei JEDEM langsamen Share und gab dem User dann nichts (stummer Timeout-Zweig)
       // -> schadete normalen Shares mehr als er den seltenen iOS-nie-settelt-Fall rettete. Zurueck auf plain await:
       // resolved wenn der User teilt (schnell oder langsam), AbortError = Abbruch (nichts), sonst Download-Fallback.
-      try{await navigator.share({title:`Einladung von ${$('kidName').value}`,text:'Du bist eingeladen!',files:[file]});track('studio_share',{source:_src});toast('Einladung geteilt ✓')}
+      const _shareData={title:`Einladung von ${$('kidName').value||'uns'}`,text:buildShareText(),files:[file]};
+      const _purl=normalizedPartyUrl();if(_purl)_shareData.url=_purl;   // K5: tippbarer RSVP-Link + Marken-Signatur im Share
+      try{await navigator.share(_shareData);track('studio_share',{source:_src});toast(_purl?'Geteilt ✓ — Zusagen laufen auf deiner Partyseite ein':'Geteilt ✓',_purl?true:false)}   // M6: Peak-Moment benennt den naechsten Schritt
       catch(err){if(err.name!=='AbortError')downloadFallback()}
     }
     else downloadFallback();
-  }catch(error){if(error.name!=='AbortError')alert('Teilen konnte nicht vorbereitet werden: '+error.message)}
+  }catch(error){if(error.name!=='AbortError')toast('Das Teilen hat gerade nicht geklappt. Bitte versuch es nochmal.',true)}   // H8: rohes alert(error.message) am Teilen-Moment -> Klartext-Toast
   finally{_sharing=false;if(card)card.style.pointerEvents='';_sb.forEach(b=>{b.classList.remove('export-busy');if(b.tagName==='BUTTON')b.removeAttribute('aria-disabled');});}
 }
 
 $('exportBtn').onclick=exportPNG;
 $('shareBtn').onclick=shareInvitation;
 $('mobileShareBtn').onclick=shareInvitation;
 
 
 // ── J4 Einladungs-Autopilot: "fertig — magst du's anders?" statt leerem Editor ──
 const AP_VARIANTS=[
   {id:'classic',label:'Klassisch',layout:'classic'},
   {id:'photo',label:'Foto-Fokus',layout:'photo',needsPhoto:true},
